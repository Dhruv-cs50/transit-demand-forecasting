# Daily Update — 2026-07-20

## Backlog status

- `main` is current through PR #42 (`946cbc8`) — confirmed via `git fetch
  origin main` / `git log`. This branch (`claude/nifty-goodall-94oq4f`)
  starts fresh from that commit.
- PR **#45** (2026-07-19, open draft) was re-verified against today's
  `main` — all five of its fixes (Ticketmaster TBD-date tz mismatch, BART
  OD batch-killing parse error, 511 `get_lines` key typo, `is_raining`
  mean-vs-mode aggregation, dead peak-hour features in
  `Processing/feature_engineering.py`) were still genuinely missing, and
  are reapplied directly in this PR. **#45 supersedes #43 and #44**
  (it already contains both of their fixes plus more); recommend merging
  this PR and closing #43, #44, #45 as superseded.
- **39 open PRs total** (#2–#24, #28–#37, #39–#41, #43–#45), only #26,
  #27, and #42 ever merged. #2–#24 predate the 2026-07-02 refactor and
  target code that no longer exists in that form — stale, recommend
  closing. #28–#37 are superseded by #38/#39–#42 — recommend closing as
  duplicates. This is the **sixth** consecutive daily log making this
  recommendation; the backlog itself, not any single remaining code bug,
  is the project's biggest standing risk — real, independently-verified
  fixes have been sitting unmerged on `main` since 2026-07-12.

## Bugs fixed today (reapplied from PR #45, independently re-verified against current `main`)

| # | File(s) | Bug | Severity |
|---|---------|-----|----------|
| 1 | `machine_learning_files/fetch_events.py` (`get_all_venues`) | Ticketmaster events with a still-TBD start time (`dates.start` has only `localDate`, no `dateTime`) produced a tz-naive timestamp via `pd.to_datetime(dt_str)`, while confirmed-time events are tz-aware (`America/Los_Angeles`) — concatenating both and calling `pd.to_datetime()` downstream raises `ValueError: Cannot mix tz-aware with tz-naive values`. Fixed by localizing the date-only branch to `America/Los_Angeles` too. | High |
| 2 | `machine_learning_files/fetch_bart_od.py` (`download_month`) | An HTTP-200 response with a non-Excel body (a bart.gov soft-404 HTML page) let `parse_bart_od_excel`'s exception propagate out of the per-URL loop, killing the entire multi-year batch job on one bad month. Wrapped the parse call in its own `try`/`except` so a bad body falls through to the next candidate URL instead. | High |
| 3 | `machine_learning_files/fetch_511_transit.py` (`get_lines`) | Read `"Content"` instead of the correct `"Contents"` top-level JSON key (the sibling `get_stops()` two methods up already uses the right key) — silently returned an empty DataFrame against a real 511 API response. Dead today (nothing calls `get_lines()` yet) but live the moment it's wired up. Fixed the key. | Low (dead today) |
| 4 | `machine_learning_files/merge_pipeline.py` (`build_feature_store`) | The boolean `is_raining` column was aggregated with `"mean"` during the monthly weather rollup, turning it into a fraction instead of using mode like its categorical sibling `weather_code`. Every downstream `==True`/`==False` comparison on `is_raining` (`evaluation/metrics.py`, `Processing/ablation.py`, `benchmarks.py`'s rainy/clear-day breakdown) silently matched almost nothing. Fixed to use the same mode logic as `weather_code`. | High |
| 5 | `machine_learning_files/zero_shot.py` (`main`, `--as-of` handling) | Same tz-strip-without-convert bug already fixed in `api.py`/`predict.py` was still present here: `as_of.tz_localize(None)` stripped a non-LA tz-aware timestamp's label directly instead of converting to LA wall-clock time first — silently shifting the anchor by hours. Fixed with the established `tz_convert("America/Los_Angeles").tz_localize(None)` pattern. | Medium |
| 6 | `Processing/feature_engineering.py` (`add_time_features`) | `is_am_peak`/`is_pm_peak`/`is_midday`/`is_late_night` were derived unconditionally from `hour_of_day`, the same dead-signal-at-monthly-cadence bug already fixed for `merge_pipeline.py`'s `add_calendar_features` but never applied to this second, independent copy — permanently `False`/`False`/`False`/`True` at this pipeline's actual monthly cadence. Applied the same `if df["hour_of_day"].nunique() > 1:` guard. | High |

All six fixes were independently re-verified by reading current `main`
before applying — not merged blindly from #45's diff.

## New bugs found today (previously unaudited)

Delegated a broad read-only sweep over files with lighter prior audit
coverage — `machine_learning_files/api.py`, `Processing/ablation.py`,
`Processing/feature_engineering.py` (beyond the peak-hour fix above),
`evaluation/metrics.py`, `models/chronos2/predict.py`,
`models/chronos2/finetune.py`, `scripts/export_website_data.py`,
`website/api-demo.html`, `configs/*.yaml`, and the pipeline's split/enrich
ordering — instructed to skip everything already covered by the ~20 prior
daily audits. Found and independently re-verified three new bugs:

| # | File(s) | Bug | Severity |
|---|---------|-----|----------|
| 7 | `machine_learning_files/merge_pipeline.py`, `Processing/feature_engineering.py` | `make_splits()` is called before `feature_engineering.py` enriches the store, so `splits/{train,val,test}.parquet` only ever carry the base-store schema — never `is_sharks_game_window`, `precip_intensity`, `in_event_catchment`, or (at monthly cadence) `is_am_peak`/`is_pm_peak`. `Processing/ablation.py::eval_on_slices()` and `evaluation/metrics.py` both default `--test`/`--actuals` to `splits/test.parquet`, so their `if "is_sharks_game_window" in merged.columns:` / `"is_game_day"` / `"precip_intensity"` guards are always false — silently skipping the Sharks-game and peak-hour/heavy-rain diagnostic slices, which `ablation.py`'s own comment calls "the primary use case". Running either script with its default paths has been producing an incomplete report (`overall` + `game_day` + `rainy_day` only) with no warning. Fixed by having `feature_engineering.py`'s `main()` re-cut `splits/{train,val,test}.parquet` from the enriched store (reusing `merge_pipeline.py`'s `make_splits`/`load_configs`) after writing `feature_store_enriched.parquet`, so the on-disk splits always match the enriched schema once the full pipeline has run. | High |
| 8 | `machine_learning_files/api.py` (`_run_forecast`) | The cached fast path (served whenever `models/chronos2/outputs/zero_shot_forecasts_*.parquet` exists — the normal state after any nightly batch run) never read the `as_of` parameter at all; it returned `.head(horizon_hours)` rows of whatever the latest cached batch contained, regardless of what anchor time the caller requested via `POST /forecast {"as_of": ...}`. A caller explicitly requesting a historical or otherwise non-default anchor silently got back "now"'s cached forecast instead, with no error. Fixed by only taking the fast path when `as_of is None` (the documented default), falling through to the slow live-Chronos path — which already handles `as_of` correctly — whenever a caller supplies one. | High |
| 9 | `Processing/feature_engineering.py` (`add_weather_features`) | `if df["timestamp"].is_monotonic_increasing: df = df.sort_values("timestamp")` — inverted condition: sorts only when the data is already sorted (a no-op) and skips sorting exactly when it's needed. Feeds `precip_3hr_sum`/`precip_6hr_sum`/`precip_24hr_sum`/`is_rain_onset`, all computed via per-station rolling windows that require chronological row order. Currently a no-op in practice (today's only caller, `build_features()` on `feature_store.parquet`, happens to already be monotonic thanks to `merge_pipeline.py`'s groupby ordering), but silently wrong the moment it's called on any differently-ordered frame — no warning either way. Fixed the inverted condition (`if not ...`). | Medium (dead today, fragile) |

## Known issues logged (not fixed today)

- The stale/duplicate PR backlog remains the actual blocker on this
  project — see "Backlog status" above. Sixth consecutive daily log
  making this recommendation; 39 open PRs, zero merges since 2026-07-12.
- `machine_learning_files/merge_pipeline.py`'s weather-timestamp tz
  handling is inconsistent between `load_weather()` (line ~144, uses
  `tz_convert(None)` — shifts to UTC wall-clock before stripping) and
  `build_feature_store()` (line ~298, uses `tz_localize(None)` — keeps
  original wall-clock digits, the pattern used everywhere else in this
  codebase). Investigated and confirmed **dead today**:
  `fetch_weather_openmeteo.py` requests Open-Meteo with
  `timezone="America/Los_Angeles"`, which returns naive local-time
  strings, so `df["timestamp"].dt.tz` is always `None` by the time either
  branch runs. Not mechanically fixed — flagging so it isn't
  re-investigated tomorrow, but the two branches would silently disagree
  by several hours if this ever becomes reachable (e.g. a future raw
  weather source that preserves UTC tz info).
- `horizon_hours` silently ignored on the live-inference fallback path in
  `api.py`/`predict.py` (flagged 2026-07-19) — still a product-decision
  item, not mechanically fixed.
- `Processing/ablation.py`'s `zero_out_groups()` still leaves
  `object`-dtype columns (currently only `transit_mode`) unneutralized —
  zero observable effect today since `transit_mode` is constant `"rail"`.

## Verified NOT bugs / no further findings

Reviewed but no new correctness bugs found: `models/chronos2/predict.py::ridership_lift()`
(relies on `station_df` being per-station-monotonic, which holds today
via `merge_pipeline.py`'s row ordering — same "coincidentally sorted, not
guaranteed" class as bug #9, but doesn't currently misbehave),
`fetch_bart_od.py::parse_bart_od_excel()`, `evaluation/metrics.py`
(`mase`/`_compute`/`_infer_seasonal_period`), `models/chronos2/finetune.py`
(`KNOWN_FUTURE_COLS` unification), `evaluation/validators.py`
(`check_timestamp_monotonic` correctly uses the non-inverted form of the
same check bug #9 had), `configs/model.yaml`, `configs/sources.yaml`,
`scripts/export_website_data.py`, `website/api-demo.html`. No CI/workflow
or `test_*.py` files exist in this repo to audit.
