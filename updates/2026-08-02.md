# Daily Update — 2026-08-02

## Backlog status (18th consecutive day — still the project's real blocker)

- `origin/main` HEAD is still `946cbc8` (the PR #42 merge, 2026-07-16) —
  verified today via the GitHub API. **Zero merges in 17 days.**
- **52 open PRs** (#2–#24, #28–#37, #39–#41, #43–#58), 2026-06-06 through
  2026-08-01. Only **six** have ever merged: #1, #26, #27, #38, #42. Every
  open PR is a `draft` — none are blocked by CI or merge conflicts
  (`PR #55`, the most complete prior fix set, shows `mergeable_state:
  clean`); they are simply sitting unreviewed.
- **PR #58** (`claude/nifty-goodall-6lw42a`, 2026-08-01) is the most
  current fix set. It reapplies the full chain back through #55 (23 bugs)
  plus 2 new one-off fixes (Lorenz-curve annotation inversion in
  `transit_eda/notebooks/eda_complete.py`, hardcoded personal `os.chdir`
  path + orphaned plots directory in the companion `.ipynb`).
- Because `main` hasn't moved, two of today's fixes below are
  independent re-discoveries of bugs already fixed on unmerged branches
  weeks ago (`fetch_511_transit.py`'s `Content`→`Contents` key, first
  fixed in PR #44 on 2026-07-18; `feature_engineering.py`'s inverted sort
  condition, first fixed in PR #46 on 2026-07-20) — cross-checked against
  those PRs' diffs after the fact and confirmed byte-identical fixes.
  Repeatedly re-finding the same bugs is the direct, measurable cost of
  the backlog not moving: every unmerged day means the next day's audit
  (this one included) partially re-does yesterday's work instead of
  covering new ground.
- **Recommendation, unchanged and now further overdue: merge PR #58 (or
  this one — fully additive on top of #58), then close #43–#57 as
  superseded and #2–#24/#28–#37 as stale/pre-refactor.** All are drafts
  with no CI or conflicts blocking them; this needs a merge decision, not
  more code.

## Bugs fixed today

| # | File(s) | Bug | Severity | Status |
|---|---------|-----|----------|--------|
| 1 | `transit_eda/notebooks/eda_complete.py` (Section 3.1, `station_2023`) | `bart_2023_wd.groupby('destination')['riders'].sum()` sums `riders` across **all 12 months and all origins** of 2023 for each destination station, but the result is stored in a column named `avg_daily_exits` and printed under headers like `"Mean exits/month"` / `"Avg Monthly Exits per Station"` — the printed values are the full-year total, ~12x too large for a monthly figure. A correctly month-averaged version (`station_annual`, built earlier in the file via `.groupby(['year','station'])['total_exits'].mean()`) already exists but is never used — evidence this is an unintentional aggregation bug, not a deliberate annual-total metric. Relative rankings/percentages/skew are unaffected by the constant scale factor, but every absolute number under "Mean exits/month" was wrong by roughly an order of magnitude. Fixed by summing within each month first, then averaging across months, mirroring the unused-but-correct `station_annual` pattern. | Medium | **New** — not present in any of the 52 open PRs (checked against #58, the most current). |
| 2 | `transit_eda/notebooks/eda_complete.py` (Section 4D, Lorenz-curve panel) | `sorted_exits`/`cumulative` are built ascending (weakest station first), so `cumulative[top20_idx]` at `stations_pct == 80` is the exit share held by the **bottom** 80% of stations, not the top 20% — the annotation labeled this value `"Top 20% of stations = …% of exits"`, backwards. | Medium | Independently re-derived; PR #58 (2026-08-01) already fixed the identical root cause with an equivalent patch — reconciled to the same fix. |
| 3 | `Processing/feature_engineering.py` (`add_weather_features`) | `if df["timestamp"].is_monotonic_increasing: df = df.sort_values("timestamp")` — inverted condition, only sorts data that's already sorted and skips sorting exactly when needed, ahead of `precip_3hr_sum`/`6hr`/`24hr`/`is_rain_onset`'s per-station rolling windows. `evaluation/validators.py`'s `check_timestamp_monotonic` uses the correct `if not ...` form of the same check, confirming this was a plain inversion. | Medium (dead today — current caller happens to already receive sorted data, per PR #46's original writeup) | Independently re-discovered; identical to PR #46's fix (2026-07-20). |
| 4 | `machine_learning_files/fetch_511_transit.py` (`get_lines`) | Used `data.get("Content", ...)` while the structurally-identical `get_stops()` two methods above uses `"Contents"` (plural) for the same NeTEx `dataObjects` envelope shape. | Low (dead code — `get_lines()` is unreachable from any CLI mode today) | Independently re-discovered; identical to PR #44's fix (2026-07-18). |
| 5 | `Dockerfile.api` | `CMD` hardcoded `--port 8080` instead of reading `$PORT`, despite the file's own comment and `README.md` both documenting that Cloud Run injects `$PORT`. Currently benign since `ENV PORT=8080` matches Cloud Run's default, but the container would keep listening on 8080 and fail health checks if the service's configured port is ever changed. Switched to shell-form `CMD` that reads `${PORT}`. | Low (latent config drift) | **New** — not covered by any prior audit log (`Dockerfile`/`Dockerfile.api`/`cloudbuild.yaml`/`app.yaml`/`firebase.json` had never been audited before today). |

All five independently re-verified against today's code before fixing (not
copied from prior PR diffs — cross-referencing against #44/#46/#58 above
happened only after finding and fixing each independently, to confirm
match and flag duplication honestly). `python3 -m py_compile` run on all
changed `.py` files; `Dockerfile.api`'s new `CMD` form manually checked
against Docker's shell-vs-exec form semantics (`${PORT}` only expands
under the shell form used here).

## New audit coverage today

Delegated a full read-only pass over files that had **never** appeared in
any of the prior 20 daily audit logs: `transit_eda/notebooks/eda_complete.py`
(1,110 lines, full read), `Dockerfile`, `Dockerfile.api`, `cloudbuild.yaml`,
`website/app.yaml`, `firebase.json`. Also manually re-read
`evaluation/metrics.py`, `evaluation/validators.py`, `Processing/ablation.py`,
`models/chronos2/predict.py`, `models/chronos2/finetune.py`, and
`scripts/export_website_data.py` in full — no further bugs found in any of
those six.

## Verified NOT bugs / no further findings

- `Dockerfile` (root, pipeline image): all `COPY` sources exist and aren't
  `.dockerignore`d; `scripts/run_pipeline.sh` (referenced in its usage
  message) exists.
- `cloudbuild.yaml`: builds the correct `Dockerfile.api`; `.gcloudignore`
  deliberately does not exclude `data/processed/*.parquet` or
  `models/chronos2/outputs/`, so the locally-generated pipeline output
  those `COPY` steps need is actually present in the Cloud Build upload.
- `website/app.yaml`: static handlers correctly match `website/index.html`
  and the catch-all fallback ordering is standard App Engine practice.
- `firebase.json`: `public: "website"` matches the real directory; `.jsx`
  files are intentionally served as raw static text for Babel-standalone,
  matching how `index.html` references them.
- `transit_eda/notebooks/eda_complete.py`: data loaders, VTA handling, and
  the rest of sections 1–5 beyond the two bugs above are internally
  consistent. `station_exits`/`station_annual` (lines ~180–191) remain
  dead code — computed but never referenced — logged below, not fixed.

## Known issues logged (not fixed today)

- **The stale/duplicate PR backlog remains the project's actual
  blocker** — 18th consecutive daily log making this same recommendation,
  now against 52 open drafts and 17 days since the last merge. This has
  stopped being a "keep an eye on it" item; every day it stays open costs
  real audit effort to re-derive already-fixed bugs (2 of today's 5
  fixes were re-discoveries, as detailed above).
- `transit_eda/notebooks/eda_complete.py` lines ~180–191: `station_exits`
  and `station_annual` are computed but never used — the correctly
  month-averaged statistic that section 3.1 should have reused instead of
  its own (buggy, now-fixed) recomputation. Left in place as harmless dead
  code rather than removed, since deleting exploratory-notebook scaffolding
  is a judgment call outside a single-day bug-fix pass.
