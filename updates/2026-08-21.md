# Daily Update — 2026-08-21

## Backlog status

`main` is still current only through PR #42 (merged 2026-07-17 — **35 days
ago**). **71 open PRs (#2–#77)** now sit unreviewed/unmerged, each
independently re-deriving and carrying forward the same accumulated fix set.
This branch was built by fast-forwarding onto **PR #77**'s tip commit
(`3d7fdc3`, 2026-08-20, the newest open PR at the time this audit started),
so every fix from #77 — and everything it carried forward from #76/#75/.../#43
— is present here.

This is now the **tenth consecutive day** logging the same recommendation.
**Recommend, unchanged:** merge this PR (or #77 directly) into `main` now,
then close the #2–#76 backlog as superseded. Every additional day multiplies
the reconciliation cost for no benefit.

## Bugs fixed today

Resolved the station-code landmine flagged unresolved on 2026-08-19/08-20
(see finding #1 below — full evidence trail there), then did a fresh,
from-scratch read of the lighter-coverage files called out for this audit:
`docs/DATA_PIPELINE.md`, `cloudbuild.yaml`, `website/app.yaml`,
`configs/model.yaml`, `configs/sources.yaml`, and
`transit_eda/notebooks/eda_complete.py` (the last of these got only a
single-section fix on 2026-08-18 and had never had a full top-to-bottom
re-read since).

| # | File(s) | Bug | Severity |
|---|---------|-----|----------|
| 1 | `scripts/export_website_data.py` (`export_ridership_actuals`, `export_forecasts`) | Confirmed the landmine flagged 2026-08-19/08-20 was real: `build_ridership_json()`/`build_meta_json()` apply `BART_CODE_TO_WEBSITE_ID` to convert the feature store's 4-letter `station_id` (`EMBR`, confirmed as the real, current convention by re-reading `machine_learning_files/merge_pipeline.py` and `fetch_bart_od.py`'s `STATION_NAMES`) into the website's `website_id` convention (`emb`) before writing `stations_ridership.json`/`stations_meta.json` — but `export_ridership_actuals()` and `export_forecasts()` wrote the raw, unmapped `station_id` straight into `ridership_actuals.json`/`forecasts.json`. Traced end-to-end: a real run of `scripts/run_pipeline.sh` today would produce `stations_ridership.json` keyed by `website_id` (e.g. `"emb"`) but `ridership_actuals.json`/`forecasts.json` keyed by the raw 4-letter code (e.g. `"EMBR"`) — an internal inconsistency in this one script's own four export functions, independent of what the currently-committed static files or the JS expect. Fixed by applying `BART_CODE_TO_WEBSITE_ID` in both functions (skipping stations with no website counterpart, matching `build_ridership_json()`'s existing skip logic), so all four exported files now share one consistent `website_id` convention. | Medium |

Verified against today's code (not assumption): read `merge_pipeline.py` line
136 (`.rename(columns={"origin": "station_id", ...})`, fed by
`fetch_bart_od.py`'s 4-letter `STATION_NAMES` keys) and confirmed the real
pipeline's `station_id` is the 4-letter code. Re-read the currently-committed
`website/data/*.json` — all four files (`ridership_actuals.json`,
`forecasts.json`, `stations_meta.json`, `stations_ridership.json`) actually
carry a *third*, older 2-letter convention (`"EM"`, `"12"` etc.), a leftover
from before PR #39's station-code migration — meaning today's committed data
was generated by an even earlier version of this script than either
convention discussed in the 2026-08-19/08-20 logs. `website/sections.jsx`
(`KEY_STATIONS`'s `dataId` field, lines 671-676) and `website/charts.jsx`
(`Calibration`, line 99) both filter on that same 2-letter convention and are
explicitly commented as matching `stations_meta.json`'s `bart_id` field —
confirmed those files remain internally self-consistent with the
currently-committed static JSON and were deliberately **not** touched today.
Python re-checked with `python3 -m py_compile scripts/export_website_data.py`
— passes.

## Verified NOT bugs / no further findings

Full fresh read, no new issues: `docs/DATA_PIPELINE.md`, `cloudbuild.yaml`,
`website/app.yaml`, `configs/model.yaml`, `configs/sources.yaml` — all
unchanged since their thorough 2026-08-17/08-18 reviews (confirmed via
`git log` on each file — no commits since), still accurate against current
code.

`transit_eda/notebooks/eda_complete.py` — full top-to-bottom read (Sections
1–5, all four figure blocks, all three hypothesis write-ups). No new
correctness bugs. Confirmed the VTA source CSVs
(`transit_eda/data/vta/Ridership_by_Route_Cumulative_Yearly.csv` and
`Bus_Light_Rail_Ridership_Averages_by_Route.csv`) do exist in this repo and
have the columns the script expects (`Route`, `LineType`, `DayofWeek`,
`Boardings`, `Year`) — but confirmed the script never reaches that code: the
BART loading section (`transit_eda/data/bart/2019|2022|2023|2024/...`) has no
`.xlsx` files in this checkout, so the `FileNotFoundError` added 2026-08-18
fires first and the VTA-loading/statistics/plotting code below it is
unreachable in this sandbox — same "needs real data" category as every prior
log, not a new bug. The `load_bart_file_v1`/`v2` row/column-orientation
question remains open and unverifiable without real source files
(unchanged carry-forward, not re-litigated today).

## Known issues logged (not fixed today)

- The backlog-consistency problem remains the project's biggest actual
  risk — see "Backlog status" above. Tenth consecutive day flagging this;
  35 days and 71 open PRs since the last merge.
- **Forward-looking note from today's Task 1 fix:** the currently-committed
  `website/data/*.json` files and `sections.jsx`/`charts.jsx` still use the
  legacy 2-letter station convention (`"EM"`, `"12"`, ...) and are
  self-consistent with each other today — deliberately left unchanged. But
  `export_website_data.py` now consistently emits the `website_id` convention
  (`"emb"`, `"12th"`, ...) across all four exported files. The next time
  someone actually runs the real pipeline and regenerates+commits
  `website/data/`, `sections.jsx`'s `KEY_STATIONS[].dataId` and
  `charts.jsx`'s `Calibration` filter (`r.station_id === 'EM'`) will need to
  be updated from the 2-letter codes to the matching `website_id` values
  (`"emb"`, `"mtg"`, `"12th"`, `"brb"`, `"frm"` for the five `KEY_STATIONS`)
  to stay in sync — flagging this now so it isn't rediscovered as a fresh
  "empty table" bug later.
- `transit_eda/notebooks/eda_complete.py`'s `load_bart_file_v1`/`v2` loaders
  may have origin/destination row/column orientation reversed vs. the
  production `fetch_bart_od.py` parser — still unverifiable without real
  source `.xlsx` files, unchanged from every prior log since 2026-07-31.
- `models/chronos2/finetune.py`'s fine-tuning path and `predict.py`'s
  zero-shot fallback path remain unexercised end-to-end (no GPU packages or
  model weights available in this sandbox), consistent with every prior log.
