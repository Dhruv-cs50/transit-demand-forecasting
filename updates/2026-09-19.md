# Daily Update — 2026-09-19

## Backlog status

Same two-queue backlog as every day since PR #83 merged, re-verified directly
against the GitHub API:

- **#2–#67 (60 open)** — the pre-#83 backlog. Already fully superseded when
  PR #83 merged into `main` on 2026-08-27. Pure dead weight; should be closed
  as superseded, not reviewed.
- **#84–#106 (23 open)** — the post-#83 backlog, one PR added per day for
  twenty-three days in a row since #83 merged (2026-08-27 through yesterday,
  2026-09-18), each carrying forward the last. This branch was built by
  carrying forward **PR #106**'s tip commit (`6d721d4`, 2026-09-18, the
  newest open PR at the time this audit started); re-verified `main` is
  unchanged since the #83 merge (still `a370a32`), so the carry-forward
  applies cleanly with no reconciliation needed.

**Recommend, unchanged from every prior day:** merge this PR (or #106
directly) into `main`, then close both stale queues (#2–#67 as
superseded-by-#83, #84–#105 as superseded-by-#106/this PR) in the same pass.
Twenty-fourth consecutive day flagging this — the backlog itself, not any
individual bug, remains the project's biggest actual risk.

## Bugs fixed today

Delegated three parallel, adversarial full re-reads split by area (data
ingestion + feature engineering; modeling + evaluation; serving/website/
config/docs), each instructed to read the last several days of
`updates/*.md` first and report only genuinely new, currently-real findings.
Data ingestion + feature engineering and modeling + evaluation both came
back clean today. Serving/website/config/docs found one new, real defect —
a follow-on to yesterday's fix in the same file:

| # | File(s) | Bug | Severity |
|---|---------|-----|----------|
| 1 | `transit_eda/notebooks/eda_complete.py` (Sections 3.1, 4A, and the Section 5 Pareto chart) | Yesterday's fix corrected the `station_2023`/`station_all_yrs` aggregation to sum-within-month-then-mean-across-months and correctly renamed/relabeled the result `avg_daily_exits` at most call sites — but five other labels for that exact same variable were left unchanged and still read "exits/month" / "Monthly Exits": the printed `Mean exits/month`/`Median exits/month` summary rows, and three chart axis/legend labels ("Avg Monthly Exits per Station", "Total Monthly Exits per Station", "Avg Monthly Exits", "Avg Monthly Exits (thousands)"). This is a self-contradiction within the same code block — the same values are labeled "daily" in one print statement and "monthly" three lines later (the Top-5/Bottom-5 print block a few lines down already correctly says "avg daily exits"). Since the underlying quantity is a genuine per-day figure (never multiplied by days-in-month), a reader relying on the mislabeled prints/charts would understate the true daily figure by roughly 22x if they mistook it for a monthly total. Fixed all five labels to say "daily" instead of "month"/"monthly", matching the file's own `avg_daily_exits` naming and its already-correct nearby prints. No computed values changed — labeling only. | Low |

## Considered but not changed

- `machine_learning_files/fetch_events.py`'s `TicketmasterClient.get_events_for_venue`: `[p.get("max") for p in priceRanges if p.get("max")]` would raise `AttributeError` if `priceRanges` ever contained a `null` *array element* (distinct from the whole key being `null`, already handled by the 2026-09-17 fix). Plausible by analogy to the classifications/segment/genre bug class already fixed, but there's no evidence Ticketmaster's schema actually emits null array elements rather than a null/absent field, and live API access is proxy-blocked here — too speculative to fix without evidence.
- `models/chronos2/predict.py::_naive_forecast()`'s `quantile_levels` parameter is accepted but never used (hardcoded 80%/120% bands regardless of configured quantiles) — real dead parameter, but no caller in this repo ever invokes it with a value other than the default `[0.1, 0.5, 0.9]` config value the hardcoded bands already approximate. Not currently reachable with a different value.
- `Processing/ablation.py::summarise_impact()`'s `pd.DataFrame(rows).sort_values(...)` would raise `KeyError` on an empty `rows` list (e.g. only one ablation config succeeds) — same already-adjudicated empty-`rows` pattern as `offline_covariate_correlation`/`event_day_impact_table`, explicitly left unfixed 2026-09-15 as low-likelihood.
- `models/chronos2/finetune.py::build_predictor()`'s `time_limit or cfg[...]` / `preset or cfg[...]` silently ignore a falsy override (e.g. `--time-limit 0`) — not a value anyone would realistically pass.
- `Processing/feature_engineering.py`'s `CA_HOLIDAYS` variable name implies California-specific holidays, but the code and its own adjacent comment correctly use `USFederalHolidayCalendar` — naming nit only, no behavioral effect.
- `configs/sources.yaml`'s `weather.variables` and `bart.key_stations` keys are unused dead config (fetch scripts hardcode their own lists) — same already-accepted dead-config class as `YEAR_MONTH_FORMAT`/`venue_proximity_hrs`.
- `website/sections.jsx`'s Section 4D scatter's "Mean Monthly Exits (avg ridership level)" axis label uses a differently-derived, non-summed per-OD-pair mean — ambiguous whether this is actually wrong, left alone rather than guess at intended semantics.

## Verified NOT bugs / no further findings

**Data ingestion + feature engineering** — full re-reads, re-executed against synthetic data under the sandbox's installed pandas 3.0.6: `fetch_511_transit.py`, `fetch_bart_od.py`, `fetch_events.py` (including the null-safe `classifications`/`segment`/`genre` handling), `fetch_weather_openmeteo.py`, `merge_pipeline.py`'s `load_transit`/`load_weather`/`load_events`/`build_feature_store`/weather-fallback/tz-handling, `fetch_pems_roads.py`, `feature_engineering.py`'s bool-dtype sites (`is_rain_onset`, `has_sharks`→`nearest_event_attendance_tier`), `configs/sources.yaml`.

**Modeling + evaluation** — full re-reads of `finetune.py`, `predict.py`, `arima.py`, `prophet_baseline.py`, `benchmarks.py`, `scheduler.py`, `zero_shot.py`, `evaluation/metrics.py`, `evaluation/validators.py`, `Processing/ablation.py`, `configs/model.yaml` — all fixes from the last several audits (NaN-poisoning, pandas-3.x dtype bugs, AutoGluon `tuning_data`/`known_covariates` wiring, quantile-column mapping, tz handling, `horizon_hours` override) confirmed still correctly in place. No new findings.

**Serving/website/config/docs** — full re-reads of `api.py`, `export_website_data.py`, `run_pipeline.sh`, `charts.jsx`, `sections.jsx`, `tweaks-panel.jsx`, `index.html`, `api-demo.html`, `transit-demo.html`, `404.html`, `app.yaml`, `styles.css`, both Dockerfiles, `cloudbuild.yaml`, `firebase.json`, `.firebaserc`, `.gcloudignore`, `docs/ARCHITECTURE.md`, `docs/DATA_PIPELINE.md`, `README.md`, `machine_learning_files/README.md` — no new findings beyond the `eda_complete.py` fix above.

## Known issues logged (not fixed today)

- The two-queue backlog remains the project's biggest actual risk — see
  "Backlog status" above. Unchanged recommendation for the twenty-fourth
  consecutive day: merge this PR (or #106) and close both stale queues.
- **Still open, needs a design decision (unchanged since 2026-09-09):**
  `models/chronos2/predict.py`'s `_finetuned_forecast()` cannot complete a
  true forward forecast at the real production default (`as_of=None`)
  because no pipeline stage ever generates forward-looking placeholder rows
  for the actual forecast horizon.
- `zero_shot.py`'s zero-shot Chronos path never gets to use weather/event
  covariates under the project's current dependency setup (base
  `ChronosPipeline` vs. covariate-supporting `Chronos2Pipeline`) — flagging
  for the project owner rather than guessing at the right tradeoff.
- `fetch_511_transit.py`'s `"Contents"`/`"Content"` key-spelling
  discrepancy between `get_stops()` and `get_lines()` — real, unresolved,
  and unverifiable without live 511.org access (proxy-blocked here).
- Three CLI `--horizon` flags (`predict.py`, `arima.py`,
  `prophet_baseline.py`) are named/documented in hours but actually consume
  raw forecast steps under the monthly-cadence config — a project-owner call
  on whether to rename for clarity.
- The BART station-code convention mismatch between `export_website_data.py`'s
  `BART_CODE_TO_WEBSITE_ID` scheme and the checked-in `website/data/*.json` +
  `sections.jsx`/`charts.jsx`'s hardcoded legacy codes remains open,
  unchanged since 2026-08-21 — a fresh pipeline run today would silently
  break the site's station lookups again.
- `transit_eda/notebooks/eda_complete.py`'s `load_bart_file_v1`/`v2` loaders'
  possible origin/destination row/column orientation reversal vs. the
  production `fetch_bart_od.py` parser remains unverifiable without real
  source `.xlsx` files, unchanged since 2026-07-31. The checked-in
  `eda_complete.ipynb` executed-output snapshot has not been re-run since
  2026-08-18 and does not reflect any of the last several weeks' `.py`
  fixes, including this week's aggregation/labeling fixes — a project-owner
  call on whether/when to re-execute and re-commit it.
- `models/chronos2/finetune.py`'s fine-tuning path and `predict.py`'s
  zero-shot fallback path remain unexercised end-to-end with real GPU
  training/inference (no GPU packages, model weights, or real `data/raw/`
  inputs available in this sandbox).
- `machine_learning_files/fetch_weather_openmeteo.py`'s query-parameter
  contract with the live Open-Meteo API remains unverifiable in this
  sandbox (outbound HTTPS to that host is proxy-blocked), unchanged since
  2026-07-16.
- `requirements.txt` pins `pandas>=2.1.0` with no upper bound; this
  sandbox's installed pandas 3.x is what exposed several recent
  dtype/NaN-handling bugs — worth a project-owner decision on pinning an
  upper bound vs. treating pandas 3.x as the supported baseline.
- `Processing/feature_engineering.py::add_weather_features`'s
  `is_raining` NaN→`False` coercion during a full weather-data outage —
  real but low-impact, considered and left unfixed 2026-09-17.
- `Processing/feature_engineering.py`'s `dist_from_diridon_km`/
  `in_event_catchment` are structurally dead — no pipeline stage ever
  produces the `lat`/`lng` columns they depend on.
- `models/baselines/fetch_pems_roads.py` is not wired into
  `merge_pipeline.py`, and its `congestion_level` vs. downstream
  `is_congested` boundary convention disagrees — unresolved, unchanged.
- `machine_learning_files/fetch_events.py`'s `avg_ticket_price` treats a
  genuine `$0` (free event) price the same as missing data — unresolved,
  unchanged.
