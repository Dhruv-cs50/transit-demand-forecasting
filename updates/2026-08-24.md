# Daily Update — 2026-08-24

## Backlog status

`main` is still current only through PR #42 (merged 2026-07-17 — **38 days
ago**). **74 open PRs (#2–#80)** now sit unreviewed/unmerged, each
independently re-deriving and carrying forward the same accumulated fix set.
This branch was built by fast-forwarding onto **PR #80**'s tip commit
(`ebdbe47`, 2026-08-23, the newest open PR at the time this audit started),
so every fix from #80 — and everything it carried forward from #79/#78/.../#43
— is present here. Re-verified the carried-forward branch still applies
cleanly against current `main` (unchanged since 2026-07-17, so no
reconciliation was needed).

This is now the **thirteenth** consecutive day logging the same
recommendation. **Recommend, unchanged:** merge this PR (or #80 directly)
into `main` now, then close the #2–#79 backlog as superseded. Every
additional day multiplies the reconciliation cost for no benefit — a human
reviewer diffing 74 individual PRs would be strictly worse than just merging
the newest tip and closing the rest.

## Bugs fixed today

Delegated a fresh, adversarial read of the files with the least-recent
independent scrutiny per the last three days' logs — `fetch_511_transit.py`,
`fetch_weather_openmeteo.py`, `fetch_pems_roads.py`, `prophet_baseline.py`,
`scheduler.py`, `arima.py`, `tweaks-panel.jsx`, `Dockerfile`/`Dockerfile.api`,
`run_pipeline.sh`, `configs/*.yaml` — plus quick sanity re-checks of
`Processing/ablation.py` (touched by yesterday's fix) and its close
collaborators (`merge_pipeline.py`, `predict.py`, `api.py`). Instructed to
first read the last three days' logs so it wouldn't waste time re-deriving
already-confirmed-clean conclusions. Found and independently re-verified one
new, previously-unflagged bug before fixing it — a documentation bug, not a
code bug:

| # | File(s) | Bug | Severity |
|---|---------|-----|----------|
| 1 | `README.md`, `machine_learning_files/README.md` | `configs/model.yaml`'s `chronos2.device` default was changed from `"mps"` to `"cpu"` in an earlier audit (git history confirms the value landed on `"cpu"`, annotated "cuda if GPU, mps for Apple Silicon, cpu for Cloud Run"), but both README files still told readers Chronos "defaults to `mps`" and to "change it to `cuda` or `cpu` on non-Apple-Silicon machines" — the exact opposite of the current default. Verified against the actual current `configs/model.yaml` line (`device: "cpu"`) before fixing, not from memory of past logs. Concretely: a developer on Apple Silicon reading this doc would believe no change is needed (when they'd actually want to switch to `mps` for speed), and a Cloud Run/Linux deployer would go looking to change a value that's already correct. | Low |

Both files updated to state the current `cpu` default and describe
switching to `mps` (Apple Silicon) or `cuda` (GPU) as the optional speedup,
rather than the reversed claim.

## Verified NOT bugs / no further findings

Full, careful reads with no bugs found today: `machine_learning_files/fetch_511_transit.py`
(retry/backoff on both `HTTPError` and generic `RequestException`, BOM-stripped
JSON parsing, `get_historical_gtfs`'s direct session-less `requests.get` call
still re-passing `api_key`, month-walk loop, CLI); `models/baselines/fetch_pems_roads.py`
(PEMS-BAY fetch/melt/congestion-flag logic, `PeMSClient` login/metadata/5-min-data
paths, `_clean_5min`'s lane-column-skip/station-vs-station-length rename
precedence — already fixed previously); `models/baselines/arima.py`
(`get_sarima_orders` frequency dispatch, `fit_auto_arima`, `run_arima_all_stations`'s
tz-matching `_as_local_ts` helper and default-`train_end`-as-cutoff pattern,
confirmed intentional and consistent with `prophet_baseline.py`'s identical,
already-audited pattern); `models/baselines/prophet_baseline.py` (full
top-to-bottom re-read, not just grep — regressor handling, monthly-seasonality
gating, `.tz_localize(None)`-on-naive-Timestamp no-op behavior);
`models/baselines/scheduler.py` (confirmed `fetch_forecast_all_stations`'s
call signature still matches the current `fetch_weather_openmeteo.py`
definition); `machine_learning_files/fetch_weather_openmeteo.py` (`_to_dataframe`
column rename/weather-code decode, historical vs. forecast orchestration);
`website/tweaks-panel.jsx` (drag/clamp/postMessage protocol, all form
controls); `Dockerfile`/`Dockerfile.api` (copied-file lists verified against
what `_run_forecast`'s fast/slow paths actually import, `requirements.txt`
coverage for `statsmodels`/`prophet`); `scripts/run_pipeline.sh` (step
ordering/arguments verified against `evaluation/validators.py`'s current
`argparse` definitions); `configs/model.yaml`/`configs/sources.yaml`
(unchanged since 2026-08-21, re-confirmed still accurate).

Quick sanity re-checks (not full re-derivation): `Processing/ablation.py`'s
`run_inference_with_config`/`run_ablation` — yesterday's `as_of`/context-window
fix is correctly in place and internally consistent with `eval_on_slices`;
`machine_learning_files/merge_pipeline.py`, `models/chronos2/predict.py`,
`machine_learning_files/api.py` — all previously-fixed tz-handling,
quantile-column selection, and fallback-crash guards remain correctly in
place.

No new Python or JSX correctness bugs were found today — a genuine
mostly-clean pass, not an under-search result. `transit_eda/notebooks/eda_complete.py`
was not re-read today since `git log` confirms zero commits since its full
2026-08-21 audit; re-deriving would just restate that day's conclusions.

## Known issues logged (not fixed today)

- The backlog-consistency problem remains the project's biggest actual
  risk — see "Backlog status" above. Thirteenth consecutive day flagging
  this; 38 days and 74 open PRs since the last merge.
- `transit_eda/notebooks/eda_complete.py`'s `load_bart_file_v1`/`v2` loaders
  may have origin/destination row/column orientation reversed vs. the
  production `fetch_bart_od.py` parser — still unverifiable without real
  source `.xlsx` files, unchanged from every prior log since 2026-07-31.
- `models/chronos2/finetune.py`'s fine-tuning path and `predict.py`'s
  zero-shot fallback path remain unexercised end-to-end (no GPU packages or
  model weights available in this sandbox), consistent with every prior log.
- `machine_learning_files/fetch_weather_openmeteo.py`'s query-parameter
  contract with the live Open-Meteo API remains unverifiable in this
  sandbox (outbound HTTPS to that host is proxy-blocked), unchanged since
  2026-07-16.
- The `website/data/*.json` station-ID convention gap flagged 2026-08-21
  remains open and unchanged: no one has re-run and re-committed the real
  pipeline output since.
