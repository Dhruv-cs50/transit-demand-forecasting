# Daily Update — 2026-09-20

## Backlog status

Same two-queue backlog as every day since PR #83 merged, re-verified directly
against the GitHub API:

- **#2–#67 (60 open)** — the pre-#83 backlog. Already fully superseded when
  PR #83 merged into `main` on 2026-08-27. Pure dead weight; should be closed
  as superseded, not reviewed.
- **#84–#107 (24 open)** — the post-#83 backlog, one PR added per day for
  twenty-four days in a row since #83 merged (2026-08-27 through yesterday,
  2026-09-19), each carrying forward the last. This branch was built by
  carrying forward **PR #107**'s tip commit (`8901ab5`, 2026-09-19, the
  newest open PR at the time this audit started); re-verified `main` is
  unchanged since the #83 merge (still `a370a32`), so the carry-forward
  applies cleanly with no reconciliation needed.

**Recommend, unchanged from every prior day:** merge this PR (or #107
directly) into `main`, then close both stale queues (#2–#67 as
superseded-by-#83, #84–#106 as superseded-by-#107/this PR) in the same pass.
Twenty-fifth consecutive day flagging this — the backlog itself, not any
individual bug, remains the project's biggest actual risk.

## Bugs fixed today

Delegated three parallel, adversarial full re-reads split by area (data
ingestion + feature engineering; modeling + evaluation; serving/website/
config/docs), each instructed to read the last several days of
`updates/*.md` first and report only genuinely new, currently-real findings.
Data ingestion + feature engineering and modeling + evaluation both came
back clean today (25th and 5th consecutive clean day for those two tracks,
respectively). Serving/website/config/docs found two new, real defects:

| # | File(s) | Bug | Severity |
|---|---------|-----|----------|
| 1 | `transit_eda/notebooks/eda_complete.py` (Section 4C, BART OD heatmap) | The 2026-09-18 fix corrected the `od_matrix` computation (sum-within-month, then mean-across-months) so it no longer inflates the annual total ~12x, and 2026-09-19's follow-up fixed five other "exits/month" mislabels of the same underlying daily figure elsewhere in the file — but this heatmap's own colorbar label was never touched by either fix and still read `'Avg Monthly Riders'`. `riders` is documented (module-level `bart_clean` docstring) as an *average daily* riders figure per station-pair/month; summing within a month is a no-op (one row per group) and averaging that across the year's months yields an avg **daily** figure, not monthly — the fixed computation's own inline comment even said so ("summing straight across all 12 months ... reported an annual total, ~12x too high") while the label one line below still claimed "Monthly". Verified via `git show` on both prior fix commits (`6d721d4`, `50e97b6`) that this specific label was out of scope for each. Fixed the label to `'Avg Daily Riders'` and rewrote the stale inline comment to state the correct semantics directly rather than describing the old (wrong) label. No computed values changed — labeling/comment only. | Low |
| 2 | `Dockerfile.api` | The file's own header comment gave `gcloud builds submit --region=us-west2 --tag us-west2-docker.pkg.dev/<PROJECT_ID>/transit-repo/transit-api:tag1` as the copy-pasteable build/push command. A bare `--tag` invocation with no `--config`/`-f` always builds the file literally named `Dockerfile` in the source root — never `Dockerfile.api` — so following this exact command would silently build and push the *other* image (the full ingestion/training pipeline, whose `CMD` just prints usage text and has no FastAPI/uvicorn) under the `transit-api:tag1` tag, breaking the live Cloud Run API on the next deploy. Confirmed against the two places that build this image correctly: `cloudbuild.yaml` (explicit `-f Dockerfile.api`) and `README.md`'s own "Cloud Run Deployment" section (`gcloud builds submit --config=cloudbuild.yaml .`) — this comment was the only build instruction in the repo that didn't actually select `Dockerfile.api`. Fixed the comment to `gcloud builds submit --config=cloudbuild.yaml .`, matching the working path, with a one-line note explaining why a bare `--tag` build doesn't work here. | Medium |

## Considered but not changed

- `website/sections.jsx`/`eda_complete.py`'s Section 4D scatter "Mean Monthly
  Exits (avg ridership level)" / "Mean Monthly Exits" axis label uses a
  differently-derived, non-summed per-OD-pair mean — already explicitly
  adjudicated 2026-09-19 as ambiguous rather than confirmed-wrong; no new
  evidence today, left alone. (Correcting the file attribution from
  yesterday's log: this label lives in `eda_complete.py`, not
  `website/sections.jsx` — the two files independently derive similar but
  not identical figures for their own audiences.)
- `website/tweaks-panel.jsx`'s `displayFont` options not all loaded via a
  Google Fonts `<link>` — already adjudicated 2026-09-13, dev-tool only, no
  new evidence.
- Unused `Gauge` component in `charts.jsx` and CSS classes left over from
  the deleted `transit-map.jsx` — dead code, no reader-facing consequence.
- `merge_pipeline.py::compute_event_features`'s unconditional
  `month_ev["game_start_hour"].mode()` (no `in .columns` guard, unlike the
  adjacent `is_sharks_game`/`is_playoff` checks) — traced the only real
  caller path (`fetch_events.py::enrich_events` unconditionally adds this
  column before any parquet is ever saved) and confirmed not reachable as a
  live bug.
- `Processing/feature_engineering.py::add_lag_features`'s hourly-scale lag
  fallback when a station has fewer than 2 months of history (empty
  `diffs.median()`) — cold-start-only scenario, not reachable with real
  multi-year production data; consistent with prior audits' "no findings"
  verdicts on this function.
- Re-confirmed, no new evidence, all previously-logged unresolved items
  below remain unchanged (see "Known issues logged").

## Verified NOT bugs / no further findings

**Data ingestion + feature engineering** — full adversarial re-reads with
live synthetic-data repros under this sandbox's pandas 3.0.6/numpy 2.4.6:
`fetch_511_transit.py`, `fetch_bart_od.py`, `fetch_events.py` (including
`enrich_events`'s event-feature enrichment), `fetch_weather_openmeteo.py`,
`merge_pipeline.py` (`load_transit`/`load_weather`/`load_events`/
`build_feature_store`/weather-fallback/tz-handling/`compute_event_features`),
`fetch_pems_roads.py`, `feature_engineering.py` (`add_event_features`,
`add_calendar_features`'s `is_holiday` dtype, `add_lag_features`,
bool-dtype sites), `configs/sources.yaml`.

**Modeling + evaluation** — full re-reads with live repros:
`finetune.py`, `predict.py`, `arima.py`, `prophet_baseline.py`,
`benchmarks.py` (`diebold_mariano_test` NaN-masking re-verified),
`scheduler.py`, `zero_shot.py`, `evaluation/metrics.py` (`mase`'s
per-station grouping re-verified with a live multi-station repro),
`evaluation/validators.py`, `Processing/ablation.py` (`zero_out_groups`'s
pandas-3.x dtype fix re-verified live), `configs/model.yaml`.

**Serving/website/config/docs** — full re-reads, no new findings beyond the
two fixes above: `api.py`, `export_website_data.py`, `run_pipeline.sh`,
`charts.jsx`, `sections.jsx`, `tweaks-panel.jsx`, `index.html`,
`api-demo.html`, `transit-demo.html` (BART=41/VTA=39 station counts
re-verified), `404.html`, `app.yaml`, `styles.css`, `Dockerfile`,
`cloudbuild.yaml`, `firebase.json`, `.firebaserc`, `.gcloudignore`,
`docs/ARCHITECTURE.md`, `docs/DATA_PIPELINE.md`, `README.md`,
`machine_learning_files/README.md`, and all of `eda_complete.py` beyond the
one fix above.

## Known issues logged (not fixed today)

- The two-queue backlog remains the project's biggest actual risk — see
  "Backlog status" above. Unchanged recommendation for the twenty-fifth
  consecutive day: merge this PR (or #107) and close both stale queues.
- **Still open, needs a design decision (unchanged since 2026-09-09):**
  `models/chronos2/predict.py`'s `_finetuned_forecast()` cannot complete a
  true forward forecast at the real production default (`as_of=None`)
  because no pipeline stage ever generates forward-looking placeholder rows
  for the actual forecast horizon.
- `zero_shot.py`'s zero-shot Chronos path never gets to use weather/event
  covariates under the project's current dependency setup (base
  `ChronosPipeline` vs. covariate-supporting `Chronos2Pipeline`) — flagging
  for the project owner rather than guessing at the right tradeoff.
- `fetch_511_transit.py`'s `"Contents"`/`"Content"` key-spelling
  discrepancy between `get_stops()` and `get_lines()` — real, unresolved,
  and unverifiable without live 511.org access (proxy-blocked here).
- Three CLI `--horizon` flags (`predict.py`, `arima.py`,
  `prophet_baseline.py`) are named/documented in hours but actually consume
  raw forecast steps under the monthly-cadence config — a project-owner call
  on whether to rename for clarity.
- The BART station-code convention mismatch between `export_website_data.py`'s
  `BART_CODE_TO_WEBSITE_ID` scheme and the checked-in `website/data/*.json` +
  `sections.jsx`/`charts.jsx`'s hardcoded legacy codes remains open,
  unchanged since 2026-08-21 — a fresh pipeline run today would silently
  break the site's station lookups again.
- `transit_eda/notebooks/eda_complete.py`'s `load_bart_file_v1`/`v2` loaders'
  possible origin/destination row/column orientation reversal vs. the
  production `fetch_bart_od.py` parser remains unverifiable without real
  source `.xlsx` files, unchanged since 2026-07-31. The checked-in
  `eda_complete.ipynb` executed-output snapshot has not been re-run since
  2026-08-18 and does not reflect any of the last several weeks' `.py`
  fixes, including this week's labeling fixes — a project-owner call on
  whether/when to re-execute and re-commit it.
- `models/chronos2/finetune.py`'s fine-tuning path and `predict.py`'s
  zero-shot fallback path remain unexercised end-to-end with real GPU
  training/inference (no GPU packages, model weights, or real `data/raw/`
  inputs available in this sandbox).
- `machine_learning_files/fetch_weather_openmeteo.py`'s query-parameter
  contract with the live Open-Meteo API remains unverifiable in this
  sandbox (outbound HTTPS to that host is proxy-blocked), unchanged since
  2026-07-16.
- `requirements.txt` pins `pandas>=2.1.0` with no upper bound; this
  sandbox's installed pandas 3.x is what exposed several recent
  dtype/NaN-handling bugs — worth a project-owner decision on pinning an
  upper bound vs. treating pandas 3.x as the supported baseline.
- `Processing/feature_engineering.py::add_weather_features`'s
  `is_raining` NaN→`False` coercion during a full weather-data outage —
  real but low-impact, considered and left unfixed 2026-09-17.
- `Processing/feature_engineering.py`'s `dist_from_diridon_km`/
  `in_event_catchment` are structurally dead — no pipeline stage ever
  produces the `lat`/`lng` columns they depend on.
- `models/baselines/fetch_pems_roads.py` is not wired into
  `merge_pipeline.py`, and its `congestion_level` vs. downstream
  `is_congested` boundary convention disagrees — unresolved, unchanged.
- `machine_learning_files/fetch_events.py`'s `avg_ticket_price` treats a
  genuine `$0` (free event) price the same as missing data — unresolved,
  unchanged.
