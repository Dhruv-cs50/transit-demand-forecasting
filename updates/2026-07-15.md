# Daily Update — 2026-07-15

## Backlog status

- `main` is current through PR #38 (merged 2026-07-12). Since then, PRs
  **#39** (2026-07-13, BART station-code mapping) and **#40** (2026-07-14,
  weather blending / dead peak-hour features / ablation schema mismatch)
  have sat open and unmerged — this is now the fourth daily log in a row to
  say so. Both were re-verified still broken on `main` today (see below).
- PRs **#1–#24** (2026-06-05–06-28): still open, still stale against the
  `main` that existed before the #26/#27 merge (2026-07-02). Recommend
  closing.
- PRs **#28–#37**: still open; their fixes are now superseded by #38's
  merge in most cases. Worth a pass to confirm and close as duplicates.
- This PR reapplies #39's and #40's verified fixes directly (cherry-picked
  their fix commits, re-authored, syntax- and logic-checked) rather than
  re-diffing the same three files from scratch, then adds four new bugs
  found in files this 40-PR backlog had not yet covered closely:
  `Processing/feature_engineering.py`, `evaluation/metrics.py`,
  `machine_learning_files/api.py`'s live-inference path, and
  `models/chronos2/finetune.py`.

**Recommend:** merge this PR (or #39+#40 individually — they're
non-overlapping with each other and with today's four new fixes) and close
#1–#24 and the now-superseded #28–#37, rather than letting a 42nd daily
audit re-diff the same handful of files again tomorrow.

## Bugs fixed today

| # | File(s) | Bug | Severity | First found in |
|---|---------|-----|----------|-----------------|
| 1 | `scripts/export_website_data.py`, `website/charts.jsx`, `website/sections.jsx`, `website/api-demo.html` | `BART_CODE_TO_WEBSITE_ID` used invented 2-letter station keys (`"EM"`, `"12"`, …) that never match the feature store's real 4-letter `station_id` values (`"EMBR"`, `"12TH"`, …, per `fetch_bart_od.py`'s `STATION_NAMES`). Left the live map's real-data export empty, the Calibration chart's station filter permanently non-matching, and the API demo's default/fallback station picker sending station IDs the live `/forecast` endpoint rejects as unknown. Re-verified still broken on `main` today; reapplied PR #39's fix (rebuilt the mapping from real 4-letter codes, updated all four call sites). | High | PR #39 (2026-07-13) |
| 2 | `machine_learning_files/merge_pipeline.py` (`build_feature_store`) | Monthly weather aggregation grouped only by `(year, month)`, blending all 7 weather-monitoring locations (including `"diridon"`, ~50mi from the Bay Area BART core) into one regional number and joining it onto every station regardless of location — every station got identical weather for a given month. Re-verified still broken on `main` today; reapplied PR #40's fix (per-station weather join for the 6 BART codes with a direct weather-station match, regional fallback for the rest). | High | PR #40 (2026-07-14) |
| 3 | `machine_learning_files/merge_pipeline.py` (`add_calendar_features`) | `is_am_peak`/`is_pm_peak` derived from `ts.dt.hour`, permanently `False` at this pipeline's actual monthly cadence (every timestamp sits at midnight). Reapplied PR #40's fix: only compute these when the timestamp column actually carries sub-daily resolution. | Medium | PR #40 (2026-07-14) |
| 4 | `Processing/ablation.py` (`run_inference_with_config`) | `TimeSeriesDataFrame.from_data_frame(...)` selected columns from `include_groups` only, discarding `zero_out_groups()`'s already-zeroed excluded-group values — every ablation config except `6_full_model` fed the fixed, fully-covariate-trained predictor a smaller, mismatched schema. Reapplied PR #40's fix: always select every covariate group's columns (using the zeroed values), keeping the schema constant across configs. | High | PR #40 (2026-07-14) |
| 5 | `Processing/feature_engineering.py` (`add_event_features`) | `is_pre_event_window`/`is_post_event_window`/`event_proximity_score`/`is_sharks_game_window`/`is_any_event_day`/`nearest_event_attendance_tier` were computed with hour-scale windows (`EVENT_APPROACH_HOURS = 3.0`, `EVENT_DEPARTURE_HOURS = 1.5`) against timestamps that are always midnight-of-month at the pipeline's actual monthly cadence — a Sharks game at 19:00 on the 15th is ~350+ hours from the month-start row, so these columns were permanently `False`/`0` on real data. Same failure class as the already-fixed dead `is_am_peak`/`is_pm_peak`, but a distinct, previously-unreported occurrence in a different file. Verified downstream impact: `evaluation/validators.py`'s `check_event_coverage` warns "check event join" at ~100% zero-score every run (misdiagnosing a granularity bug as a join bug), and `evaluation/metrics.py`'s Sharks-game WAPE breakdown — one of the project's two headline model-value narratives — was always computed on an empty slice. Fixed by falling back to a monthly event-presence proxy (mirrors `merge_pipeline.py`'s own `compute_event_features` bucketing) when the timestamp column has no sub-daily resolution; verified with synthetic data that January (a Sharks-game month) now correctly flags `is_sharks_game_window=True` where it previously always read `False`. | High | New — previously unaudited |
| 6 | `evaluation/metrics.py` (`mase`, `_compute`) | `mase()` differenced a flat `y_train` array at a hardcoded `seasonality=96` (documented as "24hrs at 15min freq") with no per-station grouping. `overall_metrics()` passes it the full multi-station training set concatenated, so with enough total rows to clear the 96-row guard, the seasonal-naive lag frequently diffs one station's ridership against a completely different station's — a fabricated-but-plausible-looking "Overall" MASE. Meanwhile `per_station_metrics()`'s per-station train slices (~24 monthly rows each) never clear the 96-row guard, so **every per-station MASE silently reads `None`**, despite `finetune.py`'s `configs/model.yaml` naming MASE as the metric AutoGluon actually optimizes for. Fixed by (a) grouping the seasonal-naive scale computation by `station_id` so the lag never crosses between series, and (b) inferring the seasonal period from the training data's actual timestamp cadence (12 for monthly, mirroring `arima.py`'s existing frequency-aware SARIMA seasonal period) instead of hardcoding 96. Verified with synthetic multi-station monthly data: per-station MASE now computes a real value instead of `None`, and the grouped scale no longer diffs across station boundaries (traced the exact old-code failure: with 4 stations × 30 monthly rows concatenated, the old lag-96 diff compared station D's month-7 ridership against station A's month-1 ridership). | High | New — previously unaudited |
| 7 | `machine_learning_files/api.py` (`_run_forecast`) | The live-Chronos `/forecast` path's `as_of` timezone handling: when the feature store is tz-naive (the actual production state) and the client supplies an `as_of` string with a non-LA UTC offset, the code called `as_of.tz_localize(None)` directly — stripping the tz label without first converting to LA wall-clock time. `POST /forecast {"as_of": "2025-06-01T08:00:00+00:00"}` (08:00 UTC = 01:00 PDT) silently anchored the forecast at "2025-06-01 08:00:00" instead of the correct 01:00, a 7-hour silent shift. Only reachable on the slow path (no cached zero-shot forecast for the station), so a real, live code path, not dead code. Fixed to match the already-correct pattern used two lines above and in `merge_pipeline.py`'s date-range filter: `tz_convert("America/Los_Angeles").tz_localize(None)`. | Medium | New — previously unaudited |
| 8 | `models/chronos2/finetune.py` (`_default_covariate_cols` vs `_get_known_covariate_cols`) | Two independently-maintained copies of the "known-future covariate" column list had drifted apart: `_get_known_covariate_cols()` — which builds the `known_covariates_names` actually passed to `TimeSeriesPredictor`, telling AutoGluon which covariates it may use for the forecast horizon — was missing `is_playoff`, `weather_code`, `cloud_cover_pct`, `precip_in`, `is_very_cold`, `is_very_hot`, `is_windy`, and `temp_deviation`, all present in `_default_covariate_cols()`'s list. Any of these columns present in training data ended up included in the `TimeSeriesDataFrame` but silently excluded from what AutoGluon is told is available at forecast time — notably `is_playoff`, whose underlying data bug (string-vs-int comparison) was only fixed on 2026-07-10/07-12, so the signal was dead for training *and* would have stayed effectively unusable to the forecast horizon even after that fix. Unified both functions on one `KNOWN_FUTURE_COLS` module-level constant so they can't drift apart again. | Medium | New — previously unaudited |

## Verified NOT bugs / no new findings

- Ran a full pass over `machine_learning_files/fetch_511_transit.py`,
  `fetch_bart_od.py`, `fetch_weather_openmeteo.py`,
  `models/baselines/fetch_pems_roads.py`, `evaluation/validators.py`
  (beyond the already-fixed index-arithmetic bug), `transit_eda/notebooks/eda_complete.py`,
  and the remaining `website/*.jsx`/`*.html` files — no additional
  verified correctness/crash bugs found beyond style/dead-code nits with no
  runtime effect (`fetch_bart_od.py`'s unused `YEAR_MONTH_FORMAT` dict,
  `evaluation/validators.py`'s unused `EXPECTED_DTYPES` constant) —
  excluded as not real bugs.

## Known issues logged (not fixed today)

- The stale/duplicate PR backlog remains the actual blocker on this
  project making progress — see "Backlog status" above. This is the
  fourth consecutive daily log making this same recommendation.
- `Processing/ablation.py`'s `zero_out_groups()` still leaves
  `object`-dtype columns (currently only `transit_mode`) unneutralized when
  their group is excluded — zero observable effect today since
  `transit_mode` is a constant `"rail"` for the current BART-only feature
  store; flagged again in case a second transit mode is ever added.
- `models/chronos2/finetune.py`'s fine-tuning path and
  `models/chronos2/predict.py`'s zero-shot fallback path have not been
  exercised end-to-end (no GPU packages or model weights available in this
  sandbox) — today's fixes were verified by reading the logic and running
  standalone synthetic-data tests against the affected functions in
  isolation, not a full pipeline run.
