# Daily Update — 2026-09-22

## Backlog status

Same two-queue backlog as every day since PR #83 merged, re-verified directly
against the GitHub API:

- **#2–#67 (60 open)** — the pre-#83 backlog. Already fully superseded when
  PR #83 merged into `main` on 2026-08-27. Pure dead weight; should be closed
  as superseded, not reviewed.
- **#84–#109 (26 open)** — the post-#83 backlog, one PR added per day for
  twenty-six days in a row since #83 merged (2026-08-27 through yesterday,
  2026-09-21), each carrying forward the last. This branch was built by
  carrying forward **PR #109**'s tip commit (`6e712ba`, 2026-09-21, the
  newest open PR at the time this audit started); re-verified `main` is
  unchanged since the #83 merge (still `a370a32`), so the carry-forward
  applies cleanly with no reconciliation needed.

**Recommend, unchanged from every prior day:** merge this PR (or #109
directly) into `main`, then close both stale queues (#2–#67 as
superseded-by-#83, #84–#108 as superseded-by-#109/this PR) in the same pass.
Twenty-seventh consecutive day flagging this — the backlog itself, not any
individual bug, remains the project's biggest actual risk.

## Bugs fixed today

Delegated three parallel, adversarial full re-reads split by area (data
ingestion + feature engineering; modeling + evaluation; serving/website/
config/docs), each instructed to read the last five days of `updates/*.md`
first and report only genuinely new, currently-real findings. Data
ingestion + feature engineering (7th consecutive clean day) and
serving/website/config/docs (7th consecutive clean day) both came back
clean today. Modeling + evaluation found one new, real defect — not a
runtime bug, but a documentation/contract defect in the data-quality gate
itself:

| # | File(s) | Bug | Severity |
|---|---------|-----|----------|
| 1 | `evaluation/validators.py` (module docstring, `MAX_IMPUTED_ROWS`/`WEATHER_MAX_AGE_HOURS`/`EXPECTED_DTYPES`) | The module's own docstring claimed this "data quality gate" catches "Imputed / forward-filled gaps that exceed safe thresholds," "Weather data staleness (forecast too old to trust)," and "wrong dtypes" (part of "Schema violations") — but none of the three backing constants (`MAX_IMPUTED_ROWS`, `WEATHER_MAX_AGE_HOURS`, `EXPECTED_DTYPES`) is referenced by any `check_*()` function or by `validate_feature_store()`'s checks list, grep-confirmed. `check_weather_coverage()` only checks null-rate, never age; no imputation-run-length or dtype check exists anywhere. Anyone trusting this docstring (or a clean `validate_feature_store(mode=STRICT)` run) before training or serving would reasonably believe these three failure modes are gated when none of them are. Did not implement the missing checks: `MAX_IMPUTED_ROWS`/`WEATHER_MAX_AGE_HOURS` need upstream columns (an imputation flag, a weather-fetch timestamp) that don't exist anywhere in the current pipeline, and `EXPECTED_DTYPES`'s own `timestamp: datetime64[ns, America/Los_Angeles]` entry contradicts this project's tz-naive-by-default feature-store convention (see `predict.py`'s `as_of` tz-handling comment) — wiring it up as-is would false-positive STRICT mode on real, correct data rather than catch a real defect. Fixed the docstring to list only what's actually implemented and added a `NOT YET WIRED UP` note beside each of the three dead constants, so a future reader doesn't assume they already gate anything. Doc/comment-only; no behavior change. | Low |

## Considered but not changed

- Implementing the three dead checks in `validators.py` themselves — each
  requires either fabricating upstream data semantics that don't exist in
  the schema (an imputation flag, a weather-fetch timestamp) or wiring up
  a constant (`EXPECTED_DTYPES`'s tz-aware timestamp expectation) that
  contradicts the rest of the codebase's tz-naive convention and would
  introduce new false-positive STRICT-mode failures on legitimate data —
  a project-owner design decision, not a safe minimal fix.
- `evaluation/metrics.py::check_ridership_anomalies`'s rolling-median spike
  window includes the current row in its own baseline (not shifted by 1) —
  non-standard, but median is robust to a single outlier in a window of
  12, so it doesn't materially change flagging behavior; too
  low-confidence/low-impact to fix.
- `Processing/ablation.py`'s unused `MetricsReport`/`_compute` imports —
  dead import only, no behavior effect.
- `sections.jsx`'s `BARTForecasts` table row-opacity dimming
  (`row.month >= '2022' ? 1 : 0.65`) — with the current checked-in
  `website/data/ridership_actuals.json` (36 months per station:
  2019, then 2022–2023), `stationActuals.slice(-12)` always yields exactly
  2023, so the `< 2022` branch never fires; the dimming is inert but
  harmless, not clearly a bug vs. vestigial styling.
- `date + pd.Timedelta(days=1)` in `feature_engineering.py::add_time_features`'s
  `is_holiday_eve` — re-verified via repro it returns a plain
  `datetime.date` and set-membership against `CA_HOLIDAYS` works
  correctly. Not a bug.
- `NHLClient.get_schedule()`'s `start_utc.tzinfo` check on a possible
  `NaT` — re-verified via repro that `pd.NaT.tzinfo` is `None`, not an
  `AttributeError`, so even the previously-flagged speculative
  "missing `startTimeUTC`" scenario doesn't crash. Still unevidenced
  whether the NHL API ever omits the field; left as the already-logged
  item, not re-flagged as worse.
- Re-confirmed, no new evidence, all previously-logged unresolved items
  below remain unchanged (see "Known issues logged").

## Verified NOT bugs / no further findings

**Data ingestion + feature engineering** (7th consecutive clean day) — full
adversarial re-reads with live synthetic-data repros under this sandbox's
pandas 3.0.6/numpy 2.4.6: `fetch_bart_od.py`, `fetch_events.py`
(`NHLClient`, `TicketmasterClient`, `enrich_events`), `fetch_511_transit.py`,
`fetch_weather_openmeteo.py`, `merge_pipeline.py` (all loaders,
`compute_event_features`, `add_calendar_features`, `build_feature_store`,
`make_splits`), `zero_shot.py`, `fetch_pems_roads.py`,
`Processing/feature_engineering.py` (all feature functions),
`Processing/ablation.py` (data-prep portions), `configs/sources.yaml`.
Re-verified all three `sorted(glob(...), key=lambda p: p.stat().st_mtime)`
call sites from yesterday's mtime-dedup fix remain correct, and that
`fetch_bart_od.py`'s per-month glob and the `weather_forecast_*` glob are
correctly left on filename-sort (non-overlapping/monotonic filenames).

**Modeling + evaluation** — full re-reads with live repros, beyond the one
fix above: `finetune.py`, `predict.py`, `arima.py`, `prophet_baseline.py`,
`benchmarks.py`, `scheduler.py`, `evaluation/metrics.py`,
`Processing/ablation.py`, `configs/model.yaml`, and the rest of
`evaluation/validators.py`. Re-verified with live pandas-3.0.6 repros:
`tz_localize(None)` on an already-naive series (`check_station_coverage`)
is a no-op, not an error; the seasonal-naive lag-index arithmetic in
`predict.py::_naive_forecast()` and `benchmarks.py::seasonal_naive_forecast()`
is off-by-one-free for the monthly cadence; `metrics.py`'s NaN-guards
(`_valid_mask`, `coverage`, `interval_width`, `mase`'s per-station
grouping) all match documented behavior. All previously-logged fixes
(NaN-poisoning, pandas-3.x dtype handling, AutoGluon `tuning_data`/
`known_covariates` wiring, quantile-column mapping, tz handling,
`horizon_hours` override, mtime-based dedup) remain correctly in place
with no regressions.

**Serving/website/config/docs** (7th consecutive clean day) — full re-reads,
each claim checked against live evidence: `api.py`, `export_website_data.py`,
`run_pipeline.sh`, `charts.jsx`, `sections.jsx`, `tweaks-panel.jsx`,
`index.html`, `api-demo.html`, `transit-demo.html` (BART=41/VTA=39 station
counts re-verified programmatically), `404.html`, `app.yaml`, `styles.css`,
`Dockerfile`, `Dockerfile.api`, `cloudbuild.yaml`, `firebase.json`,
`.firebaserc`, `.gcloudignore`, `docs/ARCHITECTURE.md`,
`docs/DATA_PIPELINE.md`, `README.md`, `machine_learning_files/README.md`,
`transit_eda/notebooks/eda_complete.py` (all 1157 lines). Cross-checked
`website/data/model_comparison.json` against `README.md`'s results table
(exact match) and confirmed `website/data/eda_plots/` contains exactly the
7 files `EDA_PLOTS` in `sections.jsx` references.

## Known issues logged (not fixed today)

- The two-queue backlog remains the project's biggest actual risk — see
  "Backlog status" above. Unchanged recommendation for the twenty-seventh
  consecutive day: merge this PR (or #109) and close both stale queues.
- `evaluation/validators.py` has no way to check weather-forecast staleness
  or imputed-row run length because the feature store carries no per-row
  "fetched at" timestamp or "was this value imputed" flag anywhere in the
  pipeline. If these checks are wanted, that plumbing needs to be added
  upstream first — a project-owner/different-area call on what columns to
  add. New today, alongside the docstring fix above.
- `EXPECTED_DTYPES`'s tz-aware `timestamp` entry should be reconciled with
  the project's actual tz-naive-by-default convention before anyone wires
  it into a real dtype check. New today.
- The checked-in `website/data/ridership_actuals.json` fixture's date
  coverage (2019 present, 2020–2021 entirely absent, then 2022–2023)
  doesn't line up with `configs/model.yaml`/`README.md`'s stated "train
  starts Jan 2021" — worth a project-owner call on whether this demo JSON
  is stale and should be regenerated from a real pipeline run, or is
  intentionally synthetic/illustrative. New today.
- **Still open, needs a design decision (unchanged since 2026-09-09):**
  `models/chronos2/predict.py`'s `_finetuned_forecast()` cannot complete a
  true forward forecast at the real production default (`as_of=None`)
  because no pipeline stage ever generates forward-looking placeholder rows
  for the actual forecast horizon.
- `zero_shot.py`'s zero-shot Chronos path never gets to use weather/event
  covariates under the project's current dependency setup (base
  `ChronosPipeline` vs. covariate-supporting `Chronos2Pipeline`) — flagging
  for the project owner rather than guessing at the right tradeoff.
- `fetch_511_transit.py`'s `"Contents"`/`"Content"` key-spelling
  discrepancy between `get_stops()` and `get_lines()` — real, unresolved,
  and unverifiable without live 511.org access (proxy-blocked here).
- `benchmarks.py::diebold_mariano_test()`'s lack of a HAC/Newey-West
  variance adjustment for autocorrelated forecast errors — a statistical
  rigor question for the project owner, not treated as a bug today.
- Three CLI `--horizon` flags/params (`predict.py`, `arima.py`,
  `prophet_baseline.py`, `scheduler.py`'s fallback path) are
  named/documented in hours but actually consume raw forecast steps under
  the monthly-cadence config — a project-owner call on whether to rename
  for clarity.
- The BART station-code convention mismatch between `export_website_data.py`'s
  `BART_CODE_TO_WEBSITE_ID` scheme and the checked-in `website/data/*.json` +
  `sections.jsx`/`charts.jsx`'s hardcoded legacy codes remains open,
  unchanged since 2026-08-21 — a fresh pipeline run today would silently
  break the site's station lookups again.
- `transit_eda/notebooks/eda_complete.py`'s `load_bart_file_v1`/`v2` loaders'
  possible origin/destination row/column orientation reversal vs. the
  production `fetch_bart_od.py` parser remains unverifiable without real
  source `.xlsx` files, unchanged since 2026-07-31. The checked-in
  `eda_complete.ipynb` executed-output snapshot has not been re-run since
  2026-08-18 and does not reflect the last several weeks' `.py` fixes.
- `models/chronos2/finetune.py`'s fine-tuning path and `predict.py`'s
  zero-shot fallback path remain unexercised end-to-end with real GPU
  training/inference (no GPU packages, model weights, or real `data/raw/`
  inputs available in this sandbox).
- `machine_learning_files/fetch_weather_openmeteo.py`'s query-parameter
  contract with the live Open-Meteo API remains unverifiable in this
  sandbox (outbound HTTPS to that host is proxy-blocked), unchanged since
  2026-07-16.
- `requirements.txt` pins `pandas>=2.1.0` with no upper bound; this
  sandbox's installed pandas 3.x is what exposed several recent
  dtype/NaN-handling bugs — worth a project-owner decision on pinning an
  upper bound vs. treating pandas 3.x as the supported baseline.
- `Processing/feature_engineering.py::add_weather_features`'s
  `is_raining` NaN→`False` coercion during a full weather-data outage —
  real but low-impact, considered and left unfixed 2026-09-17.
- `Processing/feature_engineering.py`'s `dist_from_diridon_km`/
  `in_event_catchment` are structurally dead — no pipeline stage ever
  produces the `lat`/`lng` columns they depend on.
- `models/baselines/fetch_pems_roads.py` is not wired into
  `merge_pipeline.py`, and its `congestion_level` vs. downstream
  `is_congested` boundary convention disagrees — unresolved, unchanged.
- `machine_learning_files/fetch_events.py`'s `avg_ticket_price` treats a
  genuine `$0` (free event) price the same as missing data — unresolved,
  unchanged.
