# Daily Update — 2026-07-16

## Backlog status

- `main` is current through PR #38 (merged 2026-07-12: tz-naive/aware crash,
  wrong Chronos pipeline class, `scheduler.py`'s broken import paths, the
  event-month `tz_convert` regression, `is_playoff`'s string-vs-int
  comparison, and the dead Leaflet `transit-map.jsx`). All verified still
  applied on `main` today.
- Since then, PRs **#39** (2026-07-13, BART station-code mapping), **#40**
  (2026-07-14, weather blending / dead peak-hour features / ablation schema
  mismatch), and **#41** (2026-07-15, reapplies #39+#40 plus 4 new bugs:
  monthly-cadence dead event-window features, cross-station MASE
  contamination, an `as_of` tz-conversion bug in the live `/forecast` path,
  and a drifted known-future-covariate list in `finetune.py`) have sat open
  and unmerged. This is the **fifth** consecutive daily log reporting these
  same fixes as still needed.
- PRs **#1–#24** (2026-06-05–06-28): predate the 2026-07-02 `main` refactor,
  target code that no longer exists in that form. Stale — recommend closing.
- PRs **#28–#37**: superseded by #38's merge and by #39–#41. Recommend
  closing as duplicates once #39–#41 (or this PR) lands.
- **36 open PRs total**, none merged since #38. This PR re-verifies and
  reapplies #39+#40+#41's fixes directly against current `main` (all six
  applied cleanly — `main` still has every bug they describe) rather than
  re-diffing the same files for a sixth time.

**Recommend:** merge this PR (or #39+#40+#41 directly — fully
non-overlapping with each other) and close #1–#24 and #28–#37. The backlog
itself, not any remaining code bug, is now the project's biggest risk: 36
audits deep with zero merges since 2026-07-12 means real, verified fixes are
sitting unapplied on `main` for weeks at a time.

## Bugs fixed today (reapplied and re-verified against current `main`)

| # | File(s) | Bug | Severity | First found in |
|---|---------|-----|----------|-----------------|
| 1 | `scripts/export_website_data.py`, `website/charts.jsx`, `website/sections.jsx`, `website/api-demo.html` | `BART_CODE_TO_WEBSITE_ID` used invented 2-letter station keys (`"EM"`, `"12"`, …) that never match the feature store's real 4-letter `station_id` values (`"EMBR"`, `"12TH"`, …). Left the live map's real-data export empty, the Calibration chart's station filter permanently non-matching, and the API demo's default/fallback station picker sending station IDs the live `/forecast` endpoint rejects as unknown. Still broken on `main` today; reapplied PR #39's fix. | High | PR #39 (2026-07-13) |
| 2 | `machine_learning_files/merge_pipeline.py` (`build_feature_store`) | Monthly weather aggregation grouped only by `(year, month)`, blending all 7 weather-monitoring locations into one regional number and joining it onto every station — every station got identical weather for a given month regardless of location. Still broken on `main` today; reapplied PR #40's fix (per-station join for the 6 BART codes with a direct weather-station match, regional fallback for the rest). | High | PR #40 (2026-07-14) |
| 3 | `machine_learning_files/merge_pipeline.py` (`add_calendar_features`) | `is_am_peak`/`is_pm_peak` derived from `ts.dt.hour`, permanently `False` at this pipeline's actual monthly cadence (every timestamp sits at midnight). Reapplied PR #40's fix: only compute when the timestamp column actually carries sub-daily resolution. | Medium | PR #40 (2026-07-14) |
| 4 | `Processing/ablation.py` (`run_inference_with_config`) | `TimeSeriesDataFrame.from_data_frame(...)` selected columns from `include_groups` only, discarding `zero_out_groups()`'s already-zeroed excluded-group values — every ablation config except the full-model one fed the predictor a smaller, mismatched schema than it was fine-tuned on. Reapplied PR #40's fix: always select every covariate group's columns (using the zeroed values), keeping the schema constant across configs. | High | PR #40 (2026-07-14) |
| 5 | `Processing/feature_engineering.py` (`add_event_features`) | Event-window columns (`is_pre_event_window`, `is_sharks_game_window`, `event_proximity_score`, etc.) used hour-scale windows against timestamps that are always midnight-of-month at this pipeline's actual monthly cadence — permanently `False`/`0` on real data, which also makes `evaluation/validators.py`'s event-coverage check misdiagnose a granularity bug as a join bug, and zeroes out the Sharks-game WAPE breakdown. Reapplied PR #41's fix: fall back to a monthly event-presence proxy when the timestamp column has no sub-daily resolution. | High | PR #41 (2026-07-15) |
| 6 | `evaluation/metrics.py` (`mase`, `_compute`) | `mase()` differenced a flat `y_train` array at a hardcoded `seasonality=96` with no per-station grouping; `overall_metrics()`'s multi-station training set let the seasonal-naive lag diff one station's ridership against a different station's, and `per_station_metrics()`'s ~24-row-per-station slices never cleared the 96-row guard, so every per-station MASE silently read `None` even though `configs/model.yaml` names MASE as AutoGluon's optimization target. Reapplied PR #41's fix: group the seasonal-naive scale by `station_id`, and infer the seasonal period from the training data's actual timestamp cadence (12 for monthly) instead of hardcoding 96. | High | PR #41 (2026-07-15) |
| 7 | `machine_learning_files/api.py` (`_run_forecast`) | The live-Chronos `/forecast` path's `as_of` handling called `as_of.tz_localize(None)` directly on a tz-aware, non-LA-offset timestamp — stripping the tz label without first converting to LA wall-clock time, silently shifting the anchor time (e.g. `08:00 UTC` → `08:00 naive` instead of the correct `01:00`). Reapplied PR #41's fix: `tz_convert("America/Los_Angeles").tz_localize(None)`, matching the already-correct pattern two lines above. | Medium | PR #41 (2026-07-15) |
| 8 | `models/chronos2/finetune.py` (`_default_covariate_cols` vs `_get_known_covariate_cols`) | Two independently-maintained copies of the known-future-covariate column list had drifted apart — `_get_known_covariate_cols()` (which builds what's actually passed to `TimeSeriesPredictor`) was missing `is_playoff`, `weather_code`, `cloud_cover_pct`, and five other columns present in the training-side list, so those columns were silently excluded from what AutoGluon is told is usable at forecast time even when present in training data. Reapplied PR #41's fix: unified both functions on one `KNOWN_FUTURE_COLS` module-level constant. | Medium | PR #41 (2026-07-15) |

All eight fixes were independently re-verified against today's `main` (each
"before" snippet was grep-checked to match current file contents exactly
before applying) rather than merged blindly; the underlying logic was also
re-reviewed line by line during this pass, not just copied from the prior
write-ups.

## Fresh sweep for previously-unreported bugs

Delegated a broad read-only pass over files with lighter audit coverage —
`scheduler.py`, `benchmarks.py`, `arima.py`, `prophet_baseline.py`,
`predict.py`, `zero_shot.py`, `fetch_bart_od.py`, `fetch_events.py`,
`validators.py`, `configs/*.yaml` — instructed to ignore everything already
fixed above or previously logged as verified-not-a-bug. See below for
outcome.

## Known issues logged (not fixed today)

- The stale/duplicate PR backlog remains the actual blocker on this
  project — see "Backlog status" above. This is the fifth consecutive
  daily log making this recommendation; 36 open PRs and zero merges since
  2026-07-12.
- `Processing/ablation.py`'s `zero_out_groups()` still leaves `object`-dtype
  columns (currently only `transit_mode`) unneutralized when their group is
  excluded — zero observable effect today since `transit_mode` is a
  constant `"rail"` for the current BART-only feature store; flagged again
  in case a second transit mode is ever added.
- `models/chronos2/finetune.py`'s fine-tuning path and
  `models/chronos2/predict.py`'s zero-shot fallback path have not been
  exercised end-to-end (no GPU packages or model weights available in this
  sandbox) — today's fixes were verified by code review and by matching
  the exact "before" state against current `main`, not a full pipeline run.
