# Daily Update — 2026-08-17

## Backlog status

`main` is still current only through PR #42 (merged 2026-07-17 — **31 days
ago**). **67 open PRs (#2–#73)** now sit unreviewed/unmerged. This branch was
built by fast-forwarding onto **PR #73**'s tip commit (`82991f4`,
2026-08-16, the newest open PR at the time this audit started) rather than
stale `main`, so all of #73's fixes — and everything they carried forward
from #72/#71/.../#43 — are present here. All carried-forward files were
re-checked with `python3 -m py_compile` and import cleanly.

This is now the **sixth consecutive day** logging the same recommendation.
**Recommend, unchanged:** merge this PR (or #73) into `main` now, then close
the #2–#72 backlog as superseded. Reconciling 66+ divergent branches by hand
only gets more expensive the longer this goes unmerged; a clean merge today
is the cheapest it will ever be again.

## Bugs fixed today

Ran an independent fresh sweep, deliberately re-reading `configs/*.yaml`,
`models/baselines/scheduler.py`, `prophet_baseline.py`,
`fetch_pems_roads.py`, `fetch_511_transit.py`, `fetch_weather_openmeteo.py`,
`fetch_bart_od.py`, `fetch_events.py`, all `website/*.jsx`/`*.html`,
`Processing/feature_engineering.py`, `Processing/ablation.py`,
`merge_pipeline.py`, `finetune.py`, `evaluation/metrics.py`,
`evaluation/validators.py`, `benchmarks.py`, `arima.py`, `predict.py`,
`api.py`, and `export_website_data.py` from scratch rather than trusting
prior "clean" verdicts.

| # | File(s) | Bug | Severity |
|---|---------|-----|----------|
| 1 | `machine_learning_files/zero_shot.py` (`evaluate_predictions`) | Picked the `"mean"` column ahead of the `"0.5"` quantile column whenever both were present in `pipeline.predict_df()`'s output — the inverse of the fix already applied to the three sibling call sites on the same output shape (`models/chronos2/predict.py`'s `_zeroshot_forecast`/`_finetuned_forecast`, `Processing/ablation.py`, `machine_learning_files/api.py`), each of which prefers the true `"0.5"` median column and only falls back to `"mean"` when no quantile column exists. A copy-paste-sibling bug: the fix landed everywhere except this one function, silently substituting the point-forecast mean for the true median in the WAPE/MAE/RMSE computed and saved by `python models/chronos2/zero_shot.py --as-of <date>`. Fixed to match the established pattern (prefer `0.5` quantile column, fall back to `mean`, then last column). | Medium |

## Watch-item re-checks (carried from 2026-08-16)

- **`machine_learning_files/api.py`'s `max(0.0, float(x))` NaN-quantile clamp**
  (lines ~144-146, ~210-212): still present, still unreachable/unverifiable
  in this sandbox — no cached parquet or live Chronos/AutoGluon run
  available to actually produce a NaN quantile. Unchanged from yesterday;
  worth a `math.isfinite()`-based guard (matching `export_website_data.py`'s
  pattern) if this ever gets wired to a data source that can emit NaN
  quantiles.
- **`models/chronos2/predict.py`'s `ridership_lift()` NaN `lift_pct`**:
  re-grepped every call site again today. Still only called from
  `predict.py`'s own CLI `main()` — not from `api.py`'s `/forecast`
  endpoint, `scheduler.py`, or `export_website_data.py`. Confirmed still
  unreachable from the live serving path.

## Verified NOT bugs / no further findings

Reviewed and found clean today: `configs/model.yaml`, `configs/sources.yaml`,
`models/baselines/scheduler.py`, `models/baselines/prophet_baseline.py`,
`models/baselines/fetch_pems_roads.py`,
`machine_learning_files/fetch_511_transit.py`,
`machine_learning_files/fetch_weather_openmeteo.py`,
`machine_learning_files/fetch_bart_od.py`,
`machine_learning_files/fetch_events.py`,
`Processing/feature_engineering.py`, `machine_learning_files/merge_pipeline.py`,
`models/chronos2/finetune.py`, `evaluation/metrics.py`,
`evaluation/validators.py`, `models/baselines/benchmarks.py`,
`models/baselines/arima.py`, `models/chronos2/predict.py`,
`machine_learning_files/api.py`, `scripts/export_website_data.py`,
`website/charts.jsx`, `website/tweaks-panel.jsx`, `website/index.html`,
`website/transit-demo.html`, `website/api-demo.html`, `website/404.html`,
`website/app.yaml`.

## New watch items found today (not fixed — low reachability/severity)

- **`website/sections.jsx` (~line 812-814)** — `row.p10 ? fmt(row.p10) : '—'`
  (and the same pattern for `p50`/`p90`) treats a legitimate forecast value
  of exactly `0` as falsy and renders it as `'—'` (missing) instead of `0`
  — the classic JS falsy-zero bug. Not currently reachable:
  `website/data/forecasts.json` has no zero values today, and monthly BART
  OD ridership forecasts are very unlikely to land on exactly `0`. Worth a
  `!= null` guard if this component is ever touched for another reason.
- **`Processing/ablation.py` (`event_day_impact_table`, ~lines 490-532)** —
  `game_median`/`sharks_median` can be `NaN` when the underlying slice is
  empty, but the row is still appended to the output as long as
  `normal_median > 0`, producing a `NaN` `lift_ratio` in the internal
  `event_day_lift.csv` diagnostic artifact. Not part of the live website
  JSON export pipeline; low severity, logged for awareness.

## Known issues logged (not fixed today)

- The backlog-consistency problem remains the project's biggest actual
  risk — see "Backlog status" above. Sixth consecutive day flagging this.
- `transit_eda/notebooks/eda_complete.py`'s `load_bart_file_v1`/`v2` loaders
  may have origin/destination row/column orientation reversed vs. the
  production `fetch_bart_od.py` parser — still unverifiable without real
  source `.xlsx` files, unchanged from prior logs.
- `website/sections.jsx`'s `Dataset` component has stale marketing copy
  about the training date range — a docs/copy issue, not a functional bug,
  unchanged.
- `evaluation/validators.py`'s `EXPECTED_DTYPES` dict remains unused dead
  code — no behavior impact, unchanged.
- `models/chronos2/finetune.py`'s fine-tuning path and `predict.py`'s
  zero-shot fallback path remain unexercised end-to-end (no GPU packages or
  model weights available in this sandbox), consistent with every prior log.
