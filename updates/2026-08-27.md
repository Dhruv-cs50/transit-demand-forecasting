# Daily Update — 2026-08-27

## Backlog status

`main` is still current only through PR #42 (merged 2026-07-17 — **41 days
ago**). **77 open PRs (#2–#83)** now sit unreviewed/unmerged, each carrying
forward the same accumulated fix set. This branch was built by fast-forwarding
onto **PR #83**'s tip commit (`79e4777`, 2026-08-26, the newest open PR at the
time this audit started), so every fix from #83 — and everything it carried
forward from #82/#81/.../#43 — is present here. Re-verified the carried-forward
branch still applies cleanly against current `main` (unchanged since
2026-07-17, so no reconciliation was needed).

This is now the **sixteenth** consecutive day logging the same
recommendation. **Recommend, unchanged:** merge this PR (or #83 directly) into
`main` now, then close the #2–#82 backlog as superseded.

## Bugs fixed today

A fresh adversarial read cross-checked against the last several days of
`updates/*.md` first, then focused on angles the logs show as
less-scrutinized: module docstring headers, `requirements.txt`, `__init__.py`,
and cross-file consistency of every module's self-documented run path.

Found the same bug class already fixed once in `machine_learning_files/api.py`
on 2026-08-26 ("module's own docstring header still said `serving/api.py`...
there is no `serving/` package anywhere in the repo... copying the module's
own documented run command fails with `ModuleNotFoundError`"), present in
**10 more files**: each one's header/`Usage:` block still references a legacy
`ingestion/`, `processing/`, or `evaluation/` path (or, for
`prophet_baseline.py`, the wrong filename `prophet.py`) that doesn't exist in
this repo's current layout. Anyone copy-pasting the documented `python
<path>` or `from <path> import ...` line gets `ModuleNotFoundError` / "No
such file or directory".

| # | File | Wrong path documented | Correct path |
|---|------|------------------------|---------------|
| 1 | `models/baselines/scheduler.py` | `serving/scheduler.py` | `models/baselines/scheduler.py` |
| 2 | `evaluation/validators.py` | `processing/validators.py` (incl. `from processing.validators import ...`) | `evaluation/validators.py` |
| 3 | `machine_learning_files/fetch_bart_od.py` | `ingestion/fetch_bart_od.py` | `machine_learning_files/fetch_bart_od.py` |
| 4 | `machine_learning_files/fetch_weather_openmeteo.py` | `ingestion/fetch_weather_openmeteo.py` | `machine_learning_files/fetch_weather_openmeteo.py` |
| 5 | `machine_learning_files/fetch_events.py` | `ingestion/fetch_events.py` | `machine_learning_files/fetch_events.py` |
| 6 | `machine_learning_files/merge_pipeline.py` | `processing/merge_pipeline.py` | `machine_learning_files/merge_pipeline.py` |
| 7 | `models/baselines/fetch_pems_roads.py` | `ingestion/fetch_pems_roads.py` | `models/baselines/fetch_pems_roads.py` |
| 8 | `models/baselines/benchmarks.py` | `evaluation/benchmarks.py` | `models/baselines/benchmarks.py` |
| 9 | `models/baselines/prophet_baseline.py` | `models/baselines/prophet.py` (wrong filename) | `models/baselines/prophet_baseline.py` |
| 10 | `Processing/ablation.py` | `evaluation/ablation.py` | `Processing/ablation.py` |
| 11 | `Processing/feature_engineering.py` | `processing/feature_engineering.py` (wrong case, incl. `from processing.feature_engineering import ...`) | `Processing/feature_engineering.py` |

Severity: **Low** for each (doc-only, no runtime behavior change) — consistent
with the severity assigned to the identical bug class when fixed for
`api.py` on 2026-08-26.

Verified no `ingestion/`, `processing/` (lowercase), `evaluation/benchmarks.py`,
`evaluation/ablation.py`, or `serving/` directories/files exist anywhere in
the repo before fixing, and re-`py_compile`d all 11 touched files to confirm
no syntax errors were introduced.

## Verified NOT bugs / no further findings

Also read in full today with no new bugs found: `machine_learning_files/api.py`,
`evaluation/validators.py` (logic, not just header), `models/baselines/scheduler.py`,
`cloudbuild.yaml`, `.dockerignore`, `Dockerfile`/`Dockerfile.api`,
`configs/model.yaml`/`configs/sources.yaml`, and a cross-check of
`Processing/feature_engineering.py`'s lag/rolling-feature logic and config
wiring against `api.py`/`zero_shot.py` (consistent; the permanently-absent
`context_length_hours` config key is intentional/consistent dead-parameter
behavior already relied upon identically in both call sites, not a new bug).
No test files exist in this repo to review.

## Known issues logged (not fixed today)

- The backlog-consistency problem remains the project's biggest actual
  risk — see "Backlog status" above. Sixteenth consecutive day flagging
  this; 41 days and 77 open PRs since the last merge.
- `transit_eda/notebooks/eda_complete.py`'s BART OD row/column orientation
  vs. `fetch_bart_od.py` remains unverifiable without real source `.xlsx`
  files, unchanged from every prior log since 2026-07-31.
- `models/chronos2/finetune.py`'s fine-tuning path and `predict.py`'s
  zero-shot fallback path remain unexercised end-to-end (no GPU packages or
  model weights available in this sandbox), consistent with every prior log.
- `machine_learning_files/fetch_weather_openmeteo.py`'s query-parameter
  contract with the live Open-Meteo API remains unverifiable in this
  sandbox (outbound HTTPS to that host is proxy-blocked), unchanged since
  2026-07-16.
- The `website/data/*.json` station-ID convention gap flagged 2026-08-21
  remains open and unchanged: no one has re-run and re-committed the real
  pipeline output since.
