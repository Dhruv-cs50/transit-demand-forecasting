# Daily Update — 2026-08-22

## Backlog status

`main` is still current only through PR #42 (merged 2026-07-17 — **36 days
ago**). **72 open PRs (#2–#78)** now sit unreviewed/unmerged, each
independently re-deriving and carrying forward the same accumulated fix set.
This branch was built by fast-forwarding onto **PR #78**'s tip commit
(`a8abc24`, 2026-08-21, the newest open PR at the time this audit started),
so every fix from #78 — and everything it carried forward from #77/#76/.../#43
— is present here. Re-ran `python3 -m py_compile` across every touched
Python file to confirm the carried-forward branch still compiles cleanly
against current `main` (unchanged since 2026-07-17, so no reconciliation was
needed).

This is now the **eleventh** consecutive day logging the same
recommendation. **Recommend, unchanged:** merge this PR (or #78 directly)
into `main` now, then close the #2–#77 backlog as superseded. Every
additional day multiplies the reconciliation cost for no benefit — 72 PRs is
now large enough that a human reviewer diffing them individually would be
strictly worse than just merging the newest tip and closing the rest.

## Bugs fixed today

None. Did a fresh, from-scratch, top-to-bottom read of the files with the
least-recent independent audit coverage: `evaluation/metrics.py`,
`machine_learning_files/fetch_weather_openmeteo.py`,
`models/baselines/prophet_baseline.py`, `models/baselines/scheduler.py`,
`website/tweaks-panel.jsx`, `website/index.html`, `website/api-demo.html`,
`website/404.html`, and `website/transit-demo.html`. No new correctness bugs
found in any of them — see "Verified NOT bugs" below for what was checked in
each.

## Verified NOT bugs / no further findings

- `evaluation/metrics.py` — read every metric function (`mae`, `rmse`,
  `mape`, `wape`, `smape`, `mase`, `coverage`, `interval_width`), the
  per-`station_id` grouping logic in `mase()`, `_infer_seasonal_period`'s
  cadence detection, all slice functions (`per_station`, `per_hour`,
  `per_dow`, `event_day`, `weather_day`, `transit_mode`), and
  `compute_all_metrics`/`print_report`/`save_report`. Internally consistent;
  `groupby` id-alignment in `mase()` uses matching default `RangeIndex`es
  correctly. (This file was part of PR #41's 2026-07-15 MASE fix; no
  regressions since.)
- `machine_learning_files/fetch_weather_openmeteo.py` — verified
  `configs/sources.yaml`'s `weather.station_coords` block has the `lat`/`lng`
  keys the code expects; `_fetch`'s retry/backoff, `_to_dataframe`'s column
  rename and weather-code decode, and both orchestration functions
  (`fetch_historical_all_stations`, `fetch_forecast_all_stations`) are
  correct. Could not verify the Open-Meteo query parameter names against the
  live API (outbound HTTPS to that host is blocked by this sandbox's proxy
  allowlist) — unverified rather than confirmed-clean on that one point,
  unchanged status since 2026-07-16.
- `models/baselines/prophet_baseline.py` — verified `.dt.tz_localize(None)`
  on an already-tz-naive Series is a documented no-op, not a crash, so
  `fit_station`/`predict_station`'s calls are safe. Verified
  `configs/model.yaml` has `forecast_horizon_steps: 6`, so the fallback
  `.nanos`-on-`"MS"`-offset branch in `run_prophet_all_stations` (which would
  raise `AttributeError` on a non-Tick offset) is unreachable with current
  config — latent, not live, so not flagged.
- `models/baselines/scheduler.py` — verified every import actually resolves
  against current files: `machine_learning_files.fetch_weather_openmeteo`,
  `machine_learning_files.fetch_events.fetch_all`,
  `evaluation.validators.{validate_feature_store, ValidationMode}`,
  `models.chronos2.predict.Predictor` (call site signature matches
  `forecast_all_stations(horizon_hours, as_of, output_path, force_mode)`),
  and `machine_learning_files/merge_pipeline.py`. Cron parsing for
  `"0 2 * * *"` correctly yields 2am, matching `configs/model.yaml`'s
  `serving.nightly_refresh_cron`.
- `website/tweaks-panel.jsx` — `TweakRadio`'s drag/segment math and the
  thumb's `calc()` CSS are consistent with each other; drag/clamp/message
  protocol logic in `TweaksPanel` is correct.
- `website/index.html` — tweak defaults, dark-mode/localStorage handling,
  scroll-progress bar, and CSS custom-property wiring are internally
  consistent. Confirmed it makes no direct reference to `website/data/*.json`
  (that's `sections.jsx`/`charts.jsx`'s concern, already audited previously).
- `website/api-demo.html` — cross-checked every request/response shape
  against current `machine_learning_files/api.py`: `/health`, `/stations`,
  and `POST /forecast` all match field-for-field. The 1–6 "month" slider
  mapped onto the `horizon_hours` field looks odd by name but is
  semantically correct — `api.py`'s own `_run_forecast` treats
  `horizon_hours` as a step count against monthly-cadence data, matching the
  UI's usage.
- `website/404.html` — standard, unmodified Firebase-generated 404 page;
  internally consistent.
- `website/transit-demo.html` — self-contained synthetic simulation, not
  wired to the live API or `website/data/*.json`. Verified its own internal
  claims: `BART`/`VTA` array lengths (41/39) match the sidebar labels
  exactly, every polyline station id resolves to a real station entry,
  `calcHeat`'s hour-of-day branches cover all 24 hours with no gaps, and
  day-of-week indexing is consistent throughout.

## Known issues logged (not fixed today)

- The backlog-consistency problem remains the project's biggest actual
  risk — see "Backlog status" above. Eleventh consecutive day flagging
  this; 36 days and 72 open PRs since the last merge.
- The `website/data/*.json` station-ID convention gap flagged 2026-08-21
  remains open: `export_website_data.py` now consistently emits the
  `website_id` convention across all four exported files, but the
  currently-committed static JSON and `sections.jsx`/`charts.jsx` still use
  the legacy 2-letter convention. Unchanged since no one has re-run and
  re-committed the real pipeline output.
- `transit_eda/notebooks/eda_complete.py`'s `load_bart_file_v1`/`v2` loaders
  may have origin/destination row/column orientation reversed vs. the
  production `fetch_bart_od.py` parser — still unverifiable without real
  source `.xlsx` files, unchanged from every prior log since 2026-07-31.
- `models/chronos2/finetune.py`'s fine-tuning path and `predict.py`'s
  zero-shot fallback path remain unexercised end-to-end (no GPU packages or
  model weights available in this sandbox), consistent with every prior log.
- `machine_learning_files/fetch_weather_openmeteo.py`'s query-parameter
  contract with the live Open-Meteo API remains unverifiable in this
  sandbox (outbound HTTPS to that host is proxy-blocked), unchanged since
  2026-07-16.
