# Daily Update — 2026-08-09

## Backlog status

- `main` is still at commit `946cbc8` (PR #42, merged 2026-07-16). **No pull
  request has merged in 24 days.**
- **23 open PRs, #43 through #65** (2026-07-17 through 2026-08-08), all
  based on that same `main` commit. Each one reapplies the prior day's
  unmerged fixes plus whatever new bugs that day's audit found, so the diff
  has grown every single day — PR #65 alone is a ~1,100-line diff, almost
  all of it fixes that were already correct and unmerged in #64, #63, #62...
  back to #43.
- PR #65 (2026-08-08, most recent) is the most cumulative, up-to-date
  snapshot of every verified-but-unapplied fix. It was independently
  re-checked today: its "New bugs found and fixed today" table (3 items,
  see below) matches its full diff exactly, and every other change in it is
  a faithful reapplication of #43–#64.
- PRs **#1–#24, #28–#37**: predate later refactors or are superseded by
  later PRs in this same chain. Still recommend closing as duplicates.
- **Recommend (unchanged from every daily log since 2026-07-30): merge PR
  #65 and close #43–#64 as duplicates it supersedes**, then resume daily
  audits against a `main` that actually reflects yesterday's fixes. Nothing
  in today's audit changes this — it is now the single largest risk to this
  project, well past the point where another day of re-verification adds
  information. This has been flagged in every daily log for 10 consecutive
  days.

## Today's audit

Rather than re-diff and re-stack all 23 PRs' worth of fixes into a 24th
copy of the same diff, today's audit did two more useful things:

1. **Verified PR #65's 3 newly-found bugs are real, still present on
   `main`, and not duplicates of anything in the 2026-07-16 baseline:**

   | # | File | Bug | Severity |
   |---|------|-----|----------|
   | 1 | `models/chronos2/predict.py` (`Predictor.ridership_lift`) | The "typical ridership" baseline behind the website's "+X% vs typical" card matched on `hour_of_day`/`day_of_week`, which are meaningless at this pipeline's actual monthly cadence (every timestamp is midnight-of-month) — the same dead-signal bug class already fixed in four other places, missed here. Doesn't crash; silently computes the headline number from a noisy ~3–12-row sample instead of full history. | Medium |
   | 2 | `evaluation/validators.py` (`validate_feature_store`) | An `EXPECTED_DTYPES` dict exists but nothing ever reads it — `check_dtypes()` is referenced by `scheduler.py`'s `step_validate()` but was never defined, so the pre-training schema-drift gate silently never runs. | Medium |
   | 3 | `scripts/export_website_data.py` (`build_ridership_json`) | Dead `max_ridership = agg[...].quantile(0.95)` line, computed but unused. | Low (cosmetic) |

2. **Independently re-audited the codebase for anything genuinely new**,
   focused on files with lighter prior coverage: `scripts/export_website_data.py`,
   `scripts/run_pipeline.sh`, `evaluation/validators.py`, `evaluation/metrics.py`,
   `configs/model.yaml`, `configs/sources.yaml`, `Dockerfile`, `Dockerfile.api`,
   `cloudbuild.yaml`, `machine_learning_files/merge_pipeline.py`,
   `machine_learning_files/api.py`, `machine_learning_files/fetch_weather_openmeteo.py`,
   `models/baselines/fetch_pems_roads.py`, `models/baselines/scheduler.py`,
   `models/chronos2/finetune.py`, `Processing/feature_engineering.py`, and
   `website/*`. Also grepped the whole repo for `iloc[0]` and
   `tz_localize(None)`/`tz_convert(None)` patterns, and locally verified
   `tz_localize(None)` is a no-op on already-tz-naive Series (ruling out a
   suspected issue in `validators.py` and `prophet_baseline.py`).

   **No new bugs found.** Every remaining issue visible on disk today
   (inverted `is_monotonic_increasing` check in `feature_engineering.py`,
   BART-OD double-count glob in `merge_pipeline.py`, dead
   `weather_forecast_*.parquet` glob in `load_weather`, the `Predictor`
   singleton `force_mode` caching bug, hardcoded `$PORT` in `Dockerfile.api`)
   is a currently-still-broken instance of something already found and
   fixed in the unmerged #43–#65 chain — not something new. This is
   expected: `main` hasn't moved since #42, so the backlog already contains
   every fix that exists to find.

## Bugs fixed today

None applied directly to this branch. The 3 bugs above are real and
already fixed correctly in PR #65 (the cumulative, up-to-date PR); adding a
4th copy of the same reapplication to a 24th branch would not fix anything
that isn't already fixed and waiting — it would only make the eventual
merge/rebase larger. The fix that matters today is a merge decision on
#65, not another patch.

## Known issues logged (not fixed today)

- The unmerged PR backlog (23 PRs, #43–#65, zero merges since 2026-07-16)
  is the project's only real open risk right now. See "Backlog status"
  above.
- Everything else logged as a known issue in `updates/2026-07-16.md`
  remains unchanged (fine-tuning/zero-shot paths still unexercised
  end-to-end — no GPU packages or model weights available in this
  sandbox).
