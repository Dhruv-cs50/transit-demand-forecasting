# Daily Update — 2026-09-01

## Backlog status

`main` is current through **PR #83** (merged 2026-08-27, 5 days ago).
**PRs #84, #85, #86, #87, and #88** (2026-08-27 through 2026-08-31 daily
audits) remain open/draft and unmerged on top of that — each carries
everything before it forward, so #88 is a strict superset of #84–#87 and
of everything merged in #2–#83. This branch was built on top of #88's head
(`33dd1c3`, verified clean against current `main` — no reconciliation
needed) plus today's fix below.

Five days without a merge is small next to the 40-day/76-PR backlog that
built up before #83, but it's the same pattern starting again. **Recommend:**
merge this PR (or #88 directly) into `main`, then close #84–#88 as
superseded, before this becomes another multi-week pile.

The much older **#43–#67** (2026-07-17 through 2026-08-10) predate the #83
merge and target a `main` that no longer exists in that form — still stale,
still recommend closing outright rather than merging.

## Bugs fixed today

Delegated an adversarial, line-by-line pass to a fresh reviewer, instructed
to read the last five days' logs first (2026-08-27 through 2026-08-31) so it
wouldn't re-report already-known findings, and to focus on recently-touched
files and on the covariate-schema triangle between `Processing/ablation.py`,
`models/chronos2/finetune.py`, and `models/chronos2/predict.py` — a class of
bug (independently-maintained column lists drifting apart) this repo has hit
repeatedly (2026-07-16, 2026-08-05/06/07/24/29/30/31).

| # | File(s) | Bug | Severity |
|---|---------|-----|----------|
| 1 | `Processing/ablation.py` (`COVARIATE_GROUPS["events"]`) | The 2026-08-30 audit added `hours_to_event` to `finetune.py`'s `KNOWN_FUTURE_COLS` so the model would actually use it, but `ablation.py`'s independently-maintained `COVARIATE_GROUPS["events"]` — the source of both which columns `zero_out_groups()` zeroes per ablation config *and* `all_covariate_cols`, the full column set fed to `predictor.predict()` for every config including `"6_full_model"` — never got the matching entry. `hours_to_event` was silently omitted from the input DataFrame in every ablation run, including the "full model" config meant to replicate production, understating the events group's measured impact and risking a schema mismatch against a model whose `known_covariates_names` expects the column. Verified with a direct set comparison: before the fix `finetune_cols - ablation_cols == {"hours_to_event"}`; after, the two 71-column sets match exactly. Fixed by adding `"hours_to_event"` to the `"events"` group (line 76). | Medium |

Validated with `python3 -m py_compile Processing/ablation.py`.

## Verified NOT bugs / no further findings

Full re-reads with no new issues today: `models/chronos2/finetune.py`,
`models/chronos2/predict.py` (full file), `machine_learning_files/api.py`,
`models/baselines/benchmarks.py`, `models/baselines/scheduler.py`,
`models/baselines/arima.py`, `models/baselines/prophet_baseline.py`,
`machine_learning_files/fetch_events.py`, `machine_learning_files/merge_pipeline.py`,
`Processing/feature_engineering.py`, `evaluation/metrics.py`,
`evaluation/validators.py`, `machine_learning_files/zero_shot.py`,
`scripts/export_website_data.py`.

## Considered but not changed

- `models/chronos2/finetune.py`'s `KNOWN_FUTURE_COLS` still lists
  `hours_to_next_event`, even though `feature_engineering.py`'s
  monthly-cadence path (`_add_monthly_event_features`) hardcodes it to
  `NaN` for every row at this pipeline's actual (monthly) production
  cadence — the 2026-08-30 fix added the two *populated* replacement
  columns (`hours_to_event`, `event_proximity_score`) but didn't remove the
  always-NaN original. Whether AutoGluon's `known_covariates_names`
  silently ignores an all-NaN known-future column or errors on it can't be
  verified in this sandbox (`autogluon.timeseries` isn't installed, no
  GPU/model weights available — consistent with every prior log's
  finetune/predict caveat). Left as a documented, unverified candidate
  rather than editing a model-training column list on a guess.

## Known issues logged (not fixed today)

- The five-PR backlog (#84–#88) since the last merge — see "Backlog
  status" above. A real, if small, merge-decision gap.
- The BART station-code convention mismatch between
  `export_website_data.py`'s current `website_id` scheme and the checked-in
  `website/data/*.json` + `sections.jsx`/`charts.jsx`'s hardcoded legacy
  codes remains open, unchanged since 2026-08-21 — still pending a real
  pipeline re-run and re-commit.
- `transit_eda/notebooks/eda_complete.py`'s `load_bart_file_v1`/`v2` loaders
  may have origin/destination row/column orientation reversed vs. the
  production `fetch_bart_od.py` parser — still unverifiable without real
  source `.xlsx` files, unchanged since 2026-07-31.
- `models/chronos2/finetune.py`'s fine-tuning path and `predict.py`'s
  zero-shot fallback path remain unexercised end-to-end (no GPU packages or
  model weights available in this sandbox), consistent with every prior
  log — see "Considered but not changed" above for today's specific
  instance of this gap.
- `machine_learning_files/fetch_weather_openmeteo.py`'s and
  `fetch_511_transit.py`'s query-parameter/API contracts remain
  unverifiable against the live services in this sandbox (outbound HTTPS to
  those hosts is proxy-blocked), unchanged since 2026-07-16.
