# Daily Update — 2026-07-28

## Backlog status

- `main` is current through PR #42 (`946cbc8`, merged 2026-07-16) — confirmed
  via `git log` today. This branch starts fresh from that commit, same as
  every daily-audit branch since.
- PR **#53** (2026-07-27, open draft) was re-verified against today's `main`
  — all 20 of its fixes (18 reapplied from #52, plus its own 2 new findings:
  the `model_comparison.json` stale label and the `predict.py` `lift_pct`
  format crash) were still genuinely present on `main` (`git diff
  origin/main...origin/pr53` matched cleanly for all 13 files, spot-checked
  against the actual diffs rather than trusted blindly) and are reapplied
  directly in this PR via `git checkout origin/pr53 -- <file>` for each.
- **47 open PRs** (#2–#24, #28–#37, #39–#41, #43–#53); only #26, #27, #38,
  and #42 have ever merged. #2–#24 predate the 2026-07-02 refactor and
  target code that no longer exists in that form — stale, recommend
  closing. #28–#37 are superseded by #38–#53 — recommend closing as
  duplicates. This is the **fourteenth** consecutive daily log making this
  recommendation; real, independently-verified fixes have now been sitting
  unmerged on `main` for **twelve** days running since #42.

**Recommend:** merge this PR and close #43–#53 as superseded, plus the
long-stale #2–#24/#28–#37 backlog.

## Bugs fixed today (reapplied from PR #53, independently re-verified against current `main`)

Reapplied via `git checkout origin/pr53 -- <file>` for each of the 13 files
below (each file's diff against `origin/main` was inspected first — several
spot-checked line-by-line against the actual before/after text, not just the
one-line summaries — then every changed `.py` file was re-checked with
`python3 -m py_compile`, all passing):

| # | File(s) | Bug | Severity |
|---|---------|-----|----------|
| 1 | `Processing/ablation.py` (`run_inference_with_config`) | `COVARIATE_GROUPS["weather"]` was missing `weather_code`, so "no weather" ablation configs still fed the model real `weather_code` values. `COVARIATE_GROUPS["events"]` referenced the always-NaN `hours_to_next_event`/`hours_since_last_event` instead of the real `hours_to_event` signal. | Medium |
| 2 | `Processing/ablation.py` (`run_inference_with_config`) | `.predict(ts_df, prediction_length=...)` is not a valid `TimeSeriesPredictor.predict()` kwarg, and the entire feature store (extending through the test period) was passed as `data`, pushing every forecast past the whole test window so `eval_on_slices()`'s join was empty for all 6 configs. Fixed by splitting into a context window (`data`) and ablated test-period covariates (`known_covariates`). | High |
| 3 | `Processing/feature_engineering.py` (`add_time_features`) | `is_am_peak`/`is_pm_peak`/`is_midday`/`is_late_night` derived unconditionally from `hour_of_day`, permanently dead at this pipeline's monthly cadence. Guarded behind `hour_of_day.nunique() > 1`. | High |
| 4 | `Processing/feature_engineering.py` (`add_weather_features`) | Inverted sort condition (`if is_monotonic_increasing: sort()`) ahead of rolling precipitation windows. | Medium |
| 5 | `Processing/feature_engineering.py` (`main`) + `machine_learning_files/merge_pipeline.py` | `make_splits()` ran before feature enrichment, so `splits/*.parquet` never carried event/weather diagnostic columns. Fixed by re-cutting splits from the enriched store. | High |
| 6 | `machine_learning_files/api.py` (`_run_forecast`) | Cached fast path ignored the caller's `as_of` parameter entirely. Fixed to only take the fast path when `as_of is None`. | High |
| 7 | `machine_learning_files/fetch_511_transit.py` (`get_lines`) | Read `"Content"` instead of the correct `"Contents"` top-level JSON key. | Low (dead today) |
| 8 | `machine_learning_files/fetch_bart_od.py` (`download_month`) | An HTTP-200 non-Excel body could kill the entire multi-year batch job on one bad month. Wrapped the parse call to fall through to the next candidate URL. | High |
| 9 | `machine_learning_files/fetch_events.py` (`get_events_for_venue`) | TBD-time Ticketmaster events produced a tz-naive timestamp while confirmed-time events are tz-aware, raising `ValueError` on concat. Localized the date-only branch too. | High |
| 10 | `machine_learning_files/merge_pipeline.py` (`build_feature_store`) | Boolean `is_raining` aggregated with `"mean"` instead of mode, silently breaking downstream `==True/False` filters. | High |
| 11 | `machine_learning_files/zero_shot.py` (`main`) | Same tz-strip-without-convert bug fixed elsewhere in the codebase: `.tz_localize(None)` on a non-LA tz-aware timestamp kept the wrong wall-clock digits. Fixed with `tz_convert("America/Los_Angeles").tz_localize(None)`. | Medium |
| 12 | `models/chronos2/predict.py` (`_finetuned_forecast`) | Called `.predict(ts_df, prediction_length=horizon_steps)` — invalid kwarg — and never used the `future_df` known-future covariates it received. Fixed to build `known_covariates` and drop the bad kwarg. | Critical |
| 13 | `models/chronos2/predict.py` (`ridership_lift`) | "Typical for this hour/day-of-week" baseline was meaningless at monthly cadence (hour always 0), and could silently produce `NaN` lift with no matching rows. Fixed with a same-calendar-month fallback, then an overall-median fallback, then an explicit empty-dict return on `NaN`. | Medium |
| 14 | `models/chronos2/predict.py` (`Predictor._ensure_loaded`) | When `force_mode` is passed, the method ignored whether the corresponding loader actually succeeded and unconditionally set `self._mode = force_mode` — leaving `self._predictor`/`self._pipeline` as `None` while `forecast()` dispatched into the real-model path anyway, raising `AttributeError`. Fixed to only honor `force_mode` on load success, else falls back to `"naive"` with a warning. | Medium |
| 15 | `website/charts.jsx`, `website/sections.jsx` | Both hardcoded the winning model as `'AutoETS (AutoGluon)'`, a string `benchmarks.py` has never emitted (`"AutoGluon_Ensemble"`) — the leaderboard never highlighted the winning row. Fixed both to key on `'AutoGluon_Ensemble'`. | Medium |
| 16 | `machine_learning_files/merge_pipeline.py` (`load_weather`) | The nightly scheduler's weather-forecast step writes `weather_forecast_<timestamp>.parquet`, a filename pattern `load_weather()` never globbed for — every forecast row fetched nightly was silently discarded. Fixed by also globbing `weather_forecast_*.parquet`, merging it in, de-duplicating with archive rows taking priority. | High |
| 17 | `machine_learning_files/merge_pipeline.py` (`build_feature_store`) | `base = transit_df.copy()` inherited `load_transit()`'s gapped index (its aggregate-row filter drops rows via a boolean mask with no `reset_index()`), silently misaligning the weather merge's label-based `.where()` join. Fixed by resetting `base` to a clean index before the merge. | High |
| 18 | `models/chronos2/finetune.py` (`KNOWN_FUTURE_COLS`) | Listed the always-`NaN` `hours_to_next_event` instead of `hours_to_event`, the column that actually carries the real event-count signal `zero_shot.py` already uses. Swapped both `KNOWN_FUTURE_COLS` and ablation's events group to reference `hours_to_event`. | High |
| 19 | `website/data/model_comparison.json` | Still had `"model": "AutoETS (AutoGluon)"` after the rename in bug #15 — the checked-in data file `fetch('data/model_comparison.json')` actually loads at runtime never got updated, so the rename never took effect against real data. Fixed the JSON's `"model"` value to `"AutoGluon_Ensemble"`. | Medium |
| 20 | `models/chronos2/predict.py` (`main`) | `print(f"Lift : {lift.get('lift_pct', 'N/A'):+.1f}% vs typical")` applied the `:+.1f` numeric format spec to whatever `.get()` returns; since `ridership_lift()` can now legitimately return `{}` (bug #13's fix), `.get(..., 'N/A')` frequently returns the *string* `"N/A"`, and formatting a string with `:+.1f` raises `ValueError`, crashing the CLI right after a successful forecast. Fixed by computing `lift_pct` separately and only applying the numeric format when it's not `None`. | Medium |

## New bugs found today (previously unaudited)

Fresh read-only pass over cross-file consistency after today's reapplied
diff (checked for lingering references to the renamed `hours_to_next_event`,
the old `run_inference_with_config`/`predict()` signatures, and the
`'AutoETS (AutoGluon)'` label string — found none, all consistent), plus a
broader look at files with lighter historical coverage: `scripts/`,
`configs/`, `docs/`, `website/tweaks-panel.jsx`, `website/api-demo.html`,
`website/transit-demo.html` (not previously code-reviewed beyond being
referenced), `Dockerfile*`, `cloudbuild.yaml`, and a fresh top-to-bottom read
of `models/baselines/arima.py`, `models/baselines/prophet_baseline.py`,
`models/baselines/scheduler.py`, `models/baselines/benchmarks.py`,
`evaluation/metrics.py`, `evaluation/validators.py`. Found and fixed 1 new
bug, independently re-verified before fixing:

| # | File(s) | Bug | Severity |
|---|---------|-----|----------|
| 21 | `models/chronos2/predict.py` (`main`, `--all` mode) | `forecast_all_stations()` returns a **columnless** `pd.DataFrame()` when every station's forecast fails (e.g. no model/weights installed), but `main()`'s `--all` branch unconditionally indexed `combined['station_id'].nunique()` on the result to print the summary line — raising `KeyError: 'station_id'` and masking the already-logged "No forecasts generated" error behind a confusing traceback instead of a clean exit. Guarded with `if combined.empty` before touching the column. | Medium |

Also specifically re-checked two patterns that looked similar but did **not**
hold up as bugs on closer inspection — see "Verified NOT bugs" below.

## Known issues logged (not fixed today)

- The stale/duplicate PR backlog remains the actual blocker on this
  project — see "Backlog status" above. Fourteenth consecutive daily log
  making this recommendation; 47 open PRs, zero merges since 2026-07-16.
- `scripts/export_website_data.py` computes `stations_ridership.json` /
  `stations_meta.json` with no consumer anywhere in `website/` — the live
  "Live demo" heat map (`website/transit-demo.html`) uses a fully
  hardcoded, synthetic per-station multiplier table instead. Unchanged
  since first flagged 2026-07-26.
- `evaluation/validators.py`'s `check_ridership_anomalies` uses
  `rolling(96, min_periods=4)` — a leftover 15-min-cadence assumption that
  degenerates into a near-expanding-window median at monthly granularity.
  Unchanged since first flagged 2026-07-26.
- `machine_learning_files/api.py`'s `_run_forecast` slow path and
  `models/chronos2/predict.py`'s `Predictor.forecast()` both compute
  `prediction_length`/`horizon_steps` purely from
  `configs/model.yaml`'s `forecast_horizon_steps: 6` and never apply the
  caller-supplied `horizon_hours`. Unchanged since first logged 2026-07-24.
- `evaluation/validators.py` defines `EXPECTED_DTYPES`, `MAX_IMPUTED_ROWS`,
  and `WEATHER_MAX_AGE_HOURS` but no check function ever references them —
  unimplemented scope. Unchanged since first flagged 2026-07-23.
- `Processing/feature_engineering.py`'s `add_station_features()` —
  `dist_from_diridon_km`/`in_event_catchment` remain permanently dead; no
  code path supplies per-station `lat`/`lng`. Unchanged since first flagged
  2026-07-22.
- `machine_learning_files/fetch_weather_openmeteo.py` fetches
  `humidity_pct` but `merge_pipeline.py`'s monthly aggregation never keeps
  it — dead column, silently guarded downstream.
- `Processing/ablation.py`'s `zero_out_groups()` still leaves
  `object`-dtype columns (currently only `transit_mode`) unneutralized —
  zero observable effect today since `transit_mode` is constant.
- `models/baselines/fetch_pems_roads.py` remains unwired into the
  pipeline — no runtime effect.
- `machine_learning_files/zero_shot.py`'s `evaluate_predictions()` falls
  back to `pred_df.columns[-1]` as the median column if neither `"mean"`
  nor a `"0.5"`-named column is found — and by the time it's called from
  `forecast_all_stations()`, `pred_df` already has `station_id` appended as
  its last column, so that fallback would silently try to cast station-ID
  strings to `float`. Not fixed today: under the configured
  `quantile_levels: [0.1, 0.5, 0.9]`, Chronos's `predict_df()` reliably
  emits a `0.5`-named quantile column, so this fallback path is not known
  to be reachable in practice — flagging for awareness rather than as a
  confirmed live bug.
- `models/chronos2/finetune.py`'s fine-tuning path and
  `models/chronos2/predict.py`'s zero-shot fallback path have not been
  exercised end-to-end (no GPU packages or model weights available in this
  sandbox) — today's fixes were verified by code review and by matching
  the exact "before" state against current `main`, not a full pipeline run.

## Verified NOT bugs

Reviewed with fresh eyes but no new correctness bugs found:

- `models/baselines/prophet_baseline.py:240` — `last_ds =
  pd.to_datetime(...).tz_localize(None)` called unconditionally (no
  tz-awareness guard). Looked suspicious at first glance next to today's
  other tz fixes, but `.tz_localize(None)` (unlike `.tz_convert(None)`) is
  safe to call on an already-naive value — it's a no-op, not a raise. This
  matches the distinction already documented in the 2026-07-12 log's
  "Verified NOT bugs" section; re-confirmed it still holds here.
- `models/chronos2/finetune.py:425-427` (`main`) — `scores.get('WAPE',
  'N/A'):.4f` has the same shape as today's bug #20 (numeric format spec
  applied to a possible string fallback), but unlike `ridership_lift()`,
  `predictor.evaluate(val_ts, metrics=["MASE","WAPE","MAE","RMSE"])`
  returns a dict keyed exactly by the requested metric names on success —
  there's no code path that would leave `"WAPE"` absent from `scores`
  short of `evaluate()` itself raising (which would propagate before
  reaching this line, not fall through to `.get()`'s default). Left
  unchanged; flagged in the known-issues list above only because
  fine-tuning is unexercised in this sandbox, not because the pattern is
  confirmed broken.
- `website/transit-demo.html` — read in full for the first time (only
  referenced, not code-reviewed, in prior logs). Fully self-contained
  synthetic demo (hardcoded station list + heuristic heat multipliers, no
  fetch calls to real data) — consistent with the already-known
  "unwired from real data" issue above, but no crash or logic bug in the
  JS itself.
- `website/api-demo.html`, `website/tweaks-panel.jsx` — read in full, no
  bugs found.
- `Dockerfile`, `Dockerfile.api`, `cloudbuild.yaml`, `configs/sources.yaml`,
  `configs/model.yaml`, `scripts/run_pipeline.sh` — read in full, no bugs
  found; station-coordinate keys in `sources.yaml` match
  `merge_pipeline.py`'s `STATION_TO_WEATHER_STATION` mapping exactly.
- `models/baselines/arima.py`, `models/baselines/prophet_baseline.py`,
  `models/baselines/scheduler.py`, `models/baselines/benchmarks.py`,
  `evaluation/metrics.py`, `evaluation/validators.py` — fresh full read,
  no new correctness bugs found beyond the already-logged known issues.
- Cross-file consistency after today's reapply: no remaining references to
  `hours_to_next_event` as a training/ablation covariate (the surviving
  uses in `Processing/feature_engineering.py`'s `_add_monthly_event_features`
  fallback are an intentionally-separate, still-dead placeholder pair, not
  a leftover of the renamed column); no remaining references to the old
  `'AutoETS (AutoGluon)'` model-key string anywhere except static prose
  text in `website/sections.jsx`/`charts.jsx`, which was already confirmed
  unaffected by the rename in the 2026-07-27 log.
